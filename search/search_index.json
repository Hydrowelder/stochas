{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#stochas-smart-data-orchestration","title":"stochas: Smart Data Orchestration","text":"<p><code>stochas</code> is a Python framework built to handle the complexity of Monte Carlo simulations, parametric studies, and probabilistic modeling.</p> <p>It provides a robust bridge between abstract statistical rules and concrete simulation data, ensuring your experiments are repeatable, traceable, and easy to manage.</p>"},{"location":"#installation","title":"Installation","text":"<p>Install the package via your preferred manager:</p> <code>uv</code><code>pip</code> Bash<pre><code>uv add stochas\n</code></pre> Bash<pre><code>pip install stochas\n</code></pre>"},{"location":"#core-features","title":"Core Features","text":"<ul> <li>Salted Seeding: Combines Global Seeds, Parameter Names, and Trial Numbers for unique but deterministic draws.</li> <li>Numeric Mixins: Use your data containers directly in math operations (<code>container * 5.0</code>) without manually extracting values.</li> <li>Nominal Support: Easily toggle between \"Perfect World\" (Trial 0) and \"Probabilistic World\" (Monte Carlo) results.</li> <li>Pydantic Foundation: Every component is a Pydantic model, providing out-of-the-box validation and effortless JSON serialization.</li> </ul>"},{"location":"#why-use-stochas","title":"Why use <code>stochas</code>?","text":"<p>Managing hundreds of simulation trials can quickly become a mess of manual seeds and inconsistent data. <code>stochas</code> solves this by providing:</p> <ul> <li>Repeatable Randomness: Our \"Salted Seed\" logic ensures that any specific trial can be perfectly recreated, even years later, by tying randomness to simple to set and store values.</li> <li>Smart Containers: <code>NamedValue</code> objects behave like numbers or arrays but protect your data from accidental overwrites using a state-machine logic.</li> <li>Physics-Ready Distributions: A wide range of built-in distributions (Normal, Truncated Normal, Log-Normal, etc.) that handle their own random number generators internally.</li> <li>Serialized Registries: Automatically track exactly which \"rules\" (<code>Distributions</code>) and \"results\" (<code>NamedValues</code>) were used in every trial for easy export to JSON or databases.</li> </ul>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>stochas<ul> <li>base</li> <li>base_collections</li> <li>design_variable</li> <li>distribution</li> <li>mixins</li> <li>named_value</li> </ul> </li> </ul>"},{"location":"reference/stochas/","title":"stochas","text":""},{"location":"reference/stochas/#stochas","title":"stochas","text":""},{"location":"reference/stochas/#stochas.NOMINAL_TRIAL_NUM","title":"NOMINAL_TRIAL_NUM  <code>module-attribute</code>","text":"Python<pre><code>NOMINAL_TRIAL_NUM = 0\n</code></pre> <p>Trial number definition where nominal case will be used.</p> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0trial_num </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0trial_num </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0trial_num </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.DistName","title":"DistName  <code>module-attribute</code>","text":"Python<pre><code>DistName = NewType('DistName', str)\n</code></pre> <p>Alias of string. Used to type hint a distribution.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0name </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0name </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0name </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0name </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.ValueName","title":"ValueName  <code>module-attribute</code>","text":"Python<pre><code>ValueName = NewType('ValueName', str)\n</code></pre> <p>Alias of string. Used to type hint a named value's name.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0name </li> <li> <code></code>\u00a0NamedValue <code></code>\u00a0name </li> <li> <code></code>\u00a0Val <code></code>\u00a0ref </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0name </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <ul> <li> <code></code>\u00a0NamedValue <code></code>\u00a0name </li> <li> <code></code>\u00a0Val <code></code>\u00a0ref </li> </ul> </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> <li> <code></code>\u00a0NamedValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.StochasBase","title":"StochasBase","text":"<p>               Bases: <code>BaseModel</code></p> <pre><code>\n              flowchart TD\n              stochas.StochasBase[StochasBase]\n\n              \n\n              click stochas.StochasBase href \"\" \"stochas.StochasBase\"\n            </code></pre>"},{"location":"reference/stochas/#stochas.StochasBase.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Trial number identified for this instance of the model.</p>"},{"location":"reference/stochas/#stochas.StochasBase.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Campaign seed for calculating random numbers.</p>"},{"location":"reference/stochas/#stochas.StochasBase.design","title":"design  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>design: DesignValueDict = Field(\n    default_factory=DesignValueDict\n)\n</code></pre> <p>Registry of parameters for hyperparameter tuning.</p>"},{"location":"reference/stochas/#stochas.StochasBase.dists","title":"dists  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>dists: DistributionDict = Field(\n    default_factory=DistributionDict\n)\n</code></pre> <p>Random distributions used to generate the model.</p>"},{"location":"reference/stochas/#stochas.StochasBase.named","title":"named  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>named: SerializeAsAny[NamedValueDict[NDArray]] = Field(\n    default_factory=NamedValueDict[NDArray]\n)\n</code></pre> <p>Final 'baked' values from a random draw, global override, or design study.</p>"},{"location":"reference/stochas/#stochas.StochasBase.sample_dist","title":"sample_dist","text":"Python<pre><code>sample_dist(\n    dist: Dist,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray]\n</code></pre> <p>Sets the seed and trial number of the distribution, sample, registers it and the sampled value to the model, and returns the named value.</p> <p>If the NamedValue is already registered, the registered named value is returned.</p> <p>Parameters:</p> Name Type Description Default <code>dist</code> <code>Dist</code> <p>Distribution to sample and register.</p> required <code>size</code> <code>int</code> <p>Number of samples to take. Will be embedded in the returned NamedValue. Defaults to 1.</p> <code>1</code> <code>force</code> <code>bool</code> <p>Force the sampled value into the NamedValueDict if it already exists. Defaults to False.</p> <code>False</code> <code>warn</code> <code>bool</code> <p>Whether or not to warn if there is a conflict while forcing. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>NamedValue[NDArray]</code> <p>NamedValue[NDArray]: NamedValue containing the random draw.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def sample_dist(\n    self,\n    dist: Dist,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray]:\n    \"\"\"\n    Sets the seed and trial number of the distribution, sample, registers it and the sampled value to the model, and returns the named value.\n\n    If the NamedValue is already registered, the registered named value is returned.\n\n    Args:\n        dist (Dist): Distribution to sample and register.\n        size (int, optional): Number of samples to take. Will be embedded in the returned NamedValue. Defaults to 1.\n        force (bool, optional): Force the sampled value into the NamedValueDict if it already exists. Defaults to False.\n        warn (bool, optional): Whether or not to warn if there is a conflict while forcing. Defaults to True.\n\n    Returns:\n        NamedValue[NDArray]: NamedValue containing the random draw.\n\n    \"\"\"\n    dist.with_seed(self.seed).with_trial_num(self.trial_num)\n    self.dists.update(dist)\n\n    nv = dist.sample_to_named_value(size=size)\n\n    if nv in self.named and not force:\n        # defined with global override\n        if warn:\n            logger.warning(\n                f\"NamedValue {nv.name} already registered. Returning it instead of the sampled value.\"\n            )\n        return self.named[nv.name]\n    elif nv in self.named and force:\n        # defined with global override but forced to update\n        if warn:\n            logger.warning(\n                f\"NamedValue {nv.name} already registered. Force setting it to the new value in the registry.\",\n            )\n        self.named.force_update(nv, warn=False)\n    else:\n        # standard random draw\n        self.named.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.StochasBase.with_overrides","title":"with_overrides","text":"Python<pre><code>with_overrides(overrides: NamedValueDict[NDArray]) -&gt; Self\n</code></pre> <p>Sets the NamedValueDict to the provided override.</p> <p>This is useful for manually setting some named values to be used.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_overrides(self, overrides: NamedValueDict[NDArray]) -&gt; Self:\n    \"\"\"\n    Sets the NamedValueDict to the provided override.\n\n    This is useful for manually setting some named values to be used.\n    \"\"\"\n    self.named = overrides\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.StochasBase.with_seed","title":"with_seed","text":"Python<pre><code>with_seed(seed: int | None = None) -&gt; Self\n</code></pre> <p>Sets the seed to the provided value.</p> <p>This is useful for initializaing the model.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_seed(self, seed: int | None = None) -&gt; Self:\n    \"\"\"\n    Sets the seed to the provided value.\n\n    This is useful for initializaing the model.\n    \"\"\"\n    self.seed = seed\n    for _, dist in self.dists.items():\n        dist.with_seed(seed)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.StochasBase.with_trial_num","title":"with_trial_num","text":"Python<pre><code>with_trial_num(trial_num: int) -&gt; Self\n</code></pre> <p>Sets the trial_num to the provided value.</p> <p>This is useful for initializaing the model.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_trial_num(self, trial_num: int) -&gt; Self:\n    \"\"\"\n    Sets the trial_num to the provided value.\n\n    This is useful for initializaing the model.\n    \"\"\"\n    self.trial_num = trial_num\n    for _, dist in self.dists.items():\n        dist.with_trial_num(trial_num)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.StochasBase.with_override","title":"with_override","text":"Python<pre><code>with_override(override: NamedValueDict[NDArray]) -&gt; Self\n</code></pre> <p>Sets the NamedValueDict to the provided override.</p> <p>This is useful for manually setting some named values to be used.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_override(self, override: NamedValueDict[NDArray]) -&gt; Self:\n    \"\"\"\n    Sets the NamedValueDict to the provided override.\n\n    This is useful for manually setting some named values to be used.\n    \"\"\"\n    self.named = override\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict","title":"BaseDict","text":"<p>               Bases: <code>RootModel[dict[str, T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.BaseDict[BaseDict]\n\n              \n\n              click stochas.BaseDict href \"\" \"stochas.BaseDict\"\n            </code></pre> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueDict </li> <li> <code></code>\u00a0DistributionDict </li> <li> <code></code>\u00a0NamedValueDict </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueDict </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionDict </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueDict </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.BaseDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.__contains__","title":"__contains__","text":"Python<pre><code>__contains__(key: object) -&gt; bool\n</code></pre> <p>Specifies if a key already exists in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __contains__(self, key: object) -&gt; bool:\n    \"\"\"Specifies if a key already exists in the dictionary.\"\"\"\n    return self.root.__contains__(key)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList","title":"BaseList","text":"<p>               Bases: <code>RootModel[list[T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.BaseList[BaseList]\n\n              \n\n              click stochas.BaseList href \"\" \"stochas.BaseList\"\n            </code></pre> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueList </li> <li> <code></code>\u00a0DistributionList </li> <li> <code></code>\u00a0NamedValueList </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueList </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionList </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueList </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.BaseList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/#stochas.BaseList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignBool","title":"DesignBool","text":"<p>               Bases: <code>NamedValue[bool]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.DesignBool[DesignBool]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.DesignBool\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.DesignBool\n                \n\n\n              click stochas.DesignBool href \"\" \"stochas.DesignBool\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A specific 0/1 toggle, useful for enabling/disabling branches.</p>"},{"location":"reference/stochas/#stochas.DesignBool.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/#stochas.DesignBool.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/#stochas.DesignBool.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/#stochas.DesignBool.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/#stochas.DesignBool.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/#stochas.DesignBool.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignBool.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignBool.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignCategorical","title":"DesignCategorical","text":"<p>               Bases: <code>NamedValue[T]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.DesignCategorical[DesignCategorical]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.DesignCategorical\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.DesignCategorical\n                \n\n\n              click stochas.DesignCategorical href \"\" \"stochas.DesignCategorical\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A NamedValue that represents a categorical design parameter to be optimized.</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.choices","title":"choices  <code>instance-attribute</code>","text":"Python<pre><code>choices: Sequence[T]\n</code></pre> <p>For categorical search (e.g., choices=['stiff', 'soft']).</p>"},{"location":"reference/stochas/#stochas.DesignCategorical.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignCategorical.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignCategorical.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignFloat","title":"DesignFloat","text":"<p>               Bases: <code>NamedValue[float]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.DesignFloat[DesignFloat]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.DesignFloat\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.DesignFloat\n                \n\n\n              click stochas.DesignFloat href \"\" \"stochas.DesignFloat\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A NamedValue that represents a floating point design parameter to be optimized.</p> <p>Contains bounds required for search space discovery.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/#stochas.DesignFloat.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: float\n</code></pre> <p>Lower bound for hyperparameter tuning.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: float\n</code></pre> <p>Upper bound for hyperparameter tuning.</p>"},{"location":"reference/stochas/#stochas.DesignFloat.log","title":"log  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>log: bool = False\n</code></pre> <p>Use support for log-scale search (useful for learning rates or stiffness).</p>"},{"location":"reference/stochas/#stochas.DesignFloat.step","title":"step  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>step: float | None = None\n</code></pre> <p>Discretize the search space (e.g., step=0.5).</p>"},{"location":"reference/stochas/#stochas.DesignFloat.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignFloat.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignFloat.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignInt","title":"DesignInt","text":"<p>               Bases: <code>NamedValue[int]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.DesignInt[DesignInt]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.DesignInt\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.DesignInt\n                \n\n\n              click stochas.DesignInt href \"\" \"stochas.DesignInt\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A NamedValue representing an integer design parameter (e.g., number of solver iterations).</p>"},{"location":"reference/stochas/#stochas.DesignInt.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/#stochas.DesignInt.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/#stochas.DesignInt.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/#stochas.DesignInt.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/#stochas.DesignInt.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/#stochas.DesignInt.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: int\n</code></pre> <p>Lower bound (inclusive).</p>"},{"location":"reference/stochas/#stochas.DesignInt.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: int\n</code></pre> <p>Upper bound (inclusive).</p>"},{"location":"reference/stochas/#stochas.DesignInt.step","title":"step  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>step: int = 1\n</code></pre> <p>Spacing between values.</p>"},{"location":"reference/stochas/#stochas.DesignInt.log","title":"log  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>log: bool = False\n</code></pre> <p>Whether to sample from a log-scale (useful for orders of magnitude).</p>"},{"location":"reference/stochas/#stochas.DesignInt.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignInt.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignInt.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict","title":"DesignValueDict","text":"<p>               Bases: <code>BaseDict[AnyDesignValue]</code></p> <pre><code>\n              flowchart TD\n              stochas.DesignValueDict[DesignValueDict]\n              stochas.base_collections.BaseDict[BaseDict]\n\n                              stochas.base_collections.BaseDict --&gt; stochas.DesignValueDict\n                \n\n\n              click stochas.DesignValueDict href \"\" \"stochas.DesignValueDict\"\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> <p>Dictionary specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueList <code></code>\u00a0to_named_value_dict </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0design </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0design </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueList <code></code>\u00a0to_named_value_dict </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0StochasBase <code></code>\u00a0design </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0design </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.DesignValueDict.named_value_list","title":"named_value_list  <code>property</code>","text":"Python<pre><code>named_value_list: DesignValueList\n</code></pre> <p>Converts the DesignValueDict to a DesignValueList.</p>"},{"location":"reference/stochas/#stochas.DesignValueDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.get_value","title":"get_value","text":"Python<pre><code>get_value(name: ValueName | str) -&gt; Any\n</code></pre> <p>Gets the NamedValue value of a key.</p> Source code in <code>src/stochas/design_variable.py</code> Python<pre><code>def get_value(self, name: ValueName | str) -&gt; Any:\n    \"\"\"Gets the NamedValue value of a key.\"\"\"\n    return self[name].value\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueDict.get_raw_value","title":"get_raw_value","text":"Python<pre><code>get_raw_value(name: ValueName | str) -&gt; Any | None\n</code></pre> <p>Gets the NamedValue raw value. This includes None if the value has not yet been set.</p> Source code in <code>src/stochas/design_variable.py</code> Python<pre><code>def get_raw_value(self, name: ValueName | str) -&gt; Any | None:\n    \"\"\"Gets the NamedValue raw value. This includes None if the value has not yet been set.\"\"\"\n    return self[name].stored_value\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList","title":"DesignValueList","text":"<p>               Bases: <code>BaseList[AnyDesignValue]</code></p> <pre><code>\n              flowchart TD\n              stochas.DesignValueList[DesignValueList]\n              stochas.base_collections.BaseList[BaseList]\n\n                              stochas.base_collections.BaseList --&gt; stochas.DesignValueList\n                \n\n\n              click stochas.DesignValueList href \"\" \"stochas.DesignValueList\"\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> <p>List specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0DesignValueDict <code></code>\u00a0named_value_list </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueDict <code></code>\u00a0named_value_list </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.DesignValueList.to_named_value_dict","title":"to_named_value_dict  <code>property</code>","text":"Python<pre><code>to_named_value_dict: DesignValueDict\n</code></pre> <p>Converts the DesignValueList to a DesignValueDict.</p>"},{"location":"reference/stochas/#stochas.DesignValueList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/#stochas.DesignValueList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.BernoulliDistribution","title":"BernoulliDistribution","text":"<p>               Bases: <code>Distribution[bool]</code></p> <pre><code>\n              flowchart TD\n              stochas.BernoulliDistribution[BernoulliDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.BernoulliDistribution\n                \n\n\n              click stochas.BernoulliDistribution href \"\" \"stochas.BernoulliDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a single binary trial (Success/Failure).</p> <p>The simplest distribution, used for \"on/off\" flags, such as enabling or disabling a specific sensor or randomized model feature.</p> <p>Examples:</p> Python<pre><code># 50% chance to enable gravity compensation for this trial\ndist = BernoulliDistribution(name=\"use_grav_comp\", p=0.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.p","title":"p  <code>instance-attribute</code>","text":"Python<pre><code>p: float\n</code></pre> <p>Probability of success (0.0 to 1.0).</p>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.BernoulliDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.CategoricalDistribution","title":"CategoricalDistribution","text":"<p>               Bases: <code>Distribution[T]</code></p> <pre><code>\n              flowchart TD\n              stochas.CategoricalDistribution[CategoricalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.CategoricalDistribution\n                \n\n\n              click stochas.CategoricalDistribution href \"\" \"stochas.CategoricalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a discrete distribution over a fixed set of named choices.</p> <p>Ideal for switching between discrete simulation states, such as different materials, object types, or floor textures.</p> <p>Examples:</p> Python<pre><code>class Material(StrEnum):\n    STEEL = \"steel\"\n    WOOD = \"wood\"\n\ndist = CategoricalDistribution[Material](\n    name=\"floor_material\",\n    choices={Material.STEEL: 0.8, Material.WOOD: 0.2}\n)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.choices","title":"choices  <code>instance-attribute</code>","text":"Python<pre><code>choices: dict[T, float]\n</code></pre> <p>Choices for the categorical distribution. Tuples have the format (category, probability). This guarantees each category has an associated probability.</p>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.pdf","title":"pdf","text":"Python<pre><code>pdf(x: Any) -&gt; float | NDArray[Any, float]\n</code></pre> <p>Categorical distributions have no PDF. Did you mean to use pmf?</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pdf(self, x: Any) -&gt; float | NDArray[Any, float]:\n    \"\"\"Categorical distributions have no PDF. Did you mean to use pmf?\"\"\"\n    logger.warning(DISCRETE_MSG)\n    return self.pmf(x=x)\n</code></pre>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.pmf","title":"pmf","text":"Python<pre><code>pmf(x: T) -&gt; float\n</code></pre> <p>Probability Mass Function. Returns the probability of a specific category 'x'.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pmf(self, x: T) -&gt; float:\n    \"\"\"\n    Probability Mass Function. Returns the probability of a specific category 'x'.\n    \"\"\"\n    try:\n        return self.choices[x]\n    except KeyError:\n        return 0.0\n</code></pre>"},{"location":"reference/stochas/#stochas.CategoricalDistribution.cdf","title":"cdf","text":"Python<pre><code>cdf(x: Any) -&gt; float\n</code></pre> <p>Cumulative Distribution Function.</p> Note <p>This follows the order of the 'choices' list.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def cdf(self, x: Any) -&gt; float:\n    \"\"\"\n    Cumulative Distribution Function.\n\n    Note:\n        This follows the order of the 'choices' list.\n\n    \"\"\"\n    try:\n        idx = self.categories.index(x)\n        return self._scipy.cdf(idx)\n    except ValueError:\n        return 0.0\n</code></pre>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution","title":"DiscreteUniformDistribution","text":"<p>               Bases: <code>Distribution[int]</code></p> <pre><code>\n              flowchart TD\n              stochas.DiscreteUniformDistribution[DiscreteUniformDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.DiscreteUniformDistribution\n                \n\n\n              click stochas.DiscreteUniformDistribution href \"\" \"stochas.DiscreteUniformDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a distribution where every integer in a range is equally likely.</p> <p>Use this for discrete counts like number of objects, gear teeth, or array indices where you need equal probability across the whole range.</p> <p>Examples:</p> Python<pre><code>dist = DiscreteUniformDistribution(name=name, low=-1, high=2)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.DiscreteUniformDistribution.pmf","title":"pmf","text":"Python<pre><code>pmf(k: int | ndarray) -&gt; float | ndarray\n</code></pre> <p>Probability Mass Function.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pmf(self, k: int | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"Probability Mass Function.\"\"\"\n    return self._scipy.pmf(k)\n</code></pre>"},{"location":"reference/stochas/#stochas.Distribution","title":"Distribution","text":"<p>               Bases: <code>BaseModel</code>, <code>ABC</code></p> <pre><code>\n              flowchart TD\n              stochas.Distribution[Distribution]\n\n              \n\n              click stochas.Distribution href \"\" \"stochas.Distribution\"\n            </code></pre> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution </li> <li> <code></code>\u00a0CategoricalDistribution </li> <li> <code></code>\u00a0DiscreteUniformDistribution </li> <li> <code></code>\u00a0ExponentialDistribution </li> <li> <code></code>\u00a0LogNormalDistribution </li> <li> <code></code>\u00a0NormalDistribution </li> <li> <code></code>\u00a0PermutationDistribution </li> <li> <code></code>\u00a0PoissonDistribution </li> <li> <code></code>\u00a0TriangularDistribution </li> <li> <code></code>\u00a0TruncatedNormalDistribution </li> <li> <code></code>\u00a0UniformDistribution </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution </li> <li> <code></code>\u00a0CategoricalDistribution </li> <li> <code></code>\u00a0DiscreteUniformDistribution </li> <li> <code></code>\u00a0ExponentialDistribution </li> <li> <code></code>\u00a0LogNormalDistribution </li> <li> <code></code>\u00a0NormalDistribution </li> <li> <code></code>\u00a0PermutationDistribution </li> <li> <code></code>\u00a0PoissonDistribution </li> <li> <code></code>\u00a0TriangularDistribution </li> <li> <code></code>\u00a0TruncatedNormalDistribution </li> <li> <code></code>\u00a0UniformDistribution </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.Distribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.Distribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.Distribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.Distribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.Distribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.Distribution.is_continuous","title":"is_continuous  <code>abstractmethod</code> <code>property</code>","text":"Python<pre><code>is_continuous: bool\n</code></pre> <p>Flag indicating if the distribution is continuous (True) or discrete (False).</p>"},{"location":"reference/stochas/#stochas.Distribution.draw","title":"draw  <code>abstractmethod</code>","text":"Python<pre><code>draw(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>Perform a random draw</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef draw(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"Perform a random draw\"\"\"\n    msg = f\"This method has not been implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.Distribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.Distribution.pdf","title":"pdf  <code>abstractmethod</code>","text":"Python<pre><code>pdf(x: float | ndarray) -&gt; float | ndarray\n</code></pre> <p>Probability Density Function.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef pdf(self, x: float | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"Probability Density Function.\"\"\"\n    msg = f\"This method has not been implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.Distribution.cdf","title":"cdf  <code>abstractmethod</code>","text":"Python<pre><code>cdf(x: float | ndarray) -&gt; float | ndarray\n</code></pre> <p>Cumulative Distribution Function.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef cdf(self, x: float | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"Cumulative Distribution Function.\"\"\"\n    msg = f\"This method has not been implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.Distribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.Distribution.ppf","title":"ppf  <code>abstractmethod</code>","text":"Python<pre><code>ppf(q: float | ndarray) -&gt; float | ndarray\n</code></pre> <p>Percent Point Function (Inverse of CDF). Used to find the value at a specific quantile (e.g., 0.95).</p> <p>Parameters:</p> Name Type Description Default <code>q</code> <code>float | ndarray</code> <p>Probability (0.0 to 1.0).</p> required Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef ppf(self, q: float | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"\n    Percent Point Function (Inverse of CDF). Used to find the value at a specific quantile (e.g., 0.95).\n\n    Args:\n        q: Probability (0.0 to 1.0).\n\n    \"\"\"\n    msg = f\"Method 'ppf' not implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict","title":"DistributionDict","text":"<p>               Bases: <code>BaseDict[Dist]</code></p> <pre><code>\n              flowchart TD\n              stochas.DistributionDict[DistributionDict]\n              stochas.base_collections.BaseDict[BaseDict]\n\n                              stochas.base_collections.BaseDict --&gt; stochas.DistributionDict\n                \n\n\n              click stochas.DistributionDict href \"\" \"stochas.DistributionDict\"\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> <p>Dictionary specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DistributionList <code></code>\u00a0to_distribution_dict </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionList <code></code>\u00a0to_distribution_dict </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.DistributionDict.distribution_list","title":"distribution_list  <code>property</code>","text":"Python<pre><code>distribution_list: DistributionList\n</code></pre> <p>Converts the DistributionDict to a DistributionList.</p>"},{"location":"reference/stochas/#stochas.DistributionDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.__contains__","title":"__contains__","text":"Python<pre><code>__contains__(key: object) -&gt; bool\n</code></pre> <p>Specifies if a key already exists in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __contains__(self, key: object) -&gt; bool:\n    \"\"\"Specifies if a key already exists in the dictionary.\"\"\"\n    return self.root.__contains__(key)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList","title":"DistributionList","text":"<p>               Bases: <code>BaseList[Dist]</code></p> <pre><code>\n              flowchart TD\n              stochas.DistributionList[DistributionList]\n              stochas.base_collections.BaseList[BaseList]\n\n                              stochas.base_collections.BaseList --&gt; stochas.DistributionList\n                \n\n\n              click stochas.DistributionList href \"\" \"stochas.DistributionList\"\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> <p>List specifically for distributions.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0DistributionDict <code></code>\u00a0distribution_list </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionDict <code></code>\u00a0distribution_list </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.DistributionList.to_distribution_dict","title":"to_distribution_dict  <code>property</code>","text":"Python<pre><code>to_distribution_dict: DistributionDict\n</code></pre> <p>Converts the DistributionList to a DistributionDict.</p>"},{"location":"reference/stochas/#stochas.DistributionList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistributionList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.DistType","title":"DistType","text":"<p>               Bases: <code>StrEnum</code></p> <pre><code>\n              flowchart TD\n              stochas.DistType[DistType]\n\n              \n\n              click stochas.DistType href \"\" \"stochas.DistType\"\n            </code></pre>"},{"location":"reference/stochas/#stochas.DistType.NORMAL","title":"NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>NORMAL = 'normal'\n</code></pre> <p>Normal distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.UNIFORM","title":"UNIFORM  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>UNIFORM = 'uniform'\n</code></pre> <p>Uniform distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.DISCRETE_UNIFORM","title":"DISCRETE_UNIFORM  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>DISCRETE_UNIFORM = 'discrete_uniform'\n</code></pre> <p>Discrete uniform distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.CATEGORICAL","title":"CATEGORICAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>CATEGORICAL = 'categorical'\n</code></pre> <p>Categorical distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.PERMUTATION","title":"PERMUTATION  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>PERMUTATION = 'permutation'\n</code></pre> <p>Permutation distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.TRIANGULAR","title":"TRIANGULAR  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>TRIANGULAR = 'triangular'\n</code></pre> <p>Triangular distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.TRUNCATED_NORMAL","title":"TRUNCATED_NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>TRUNCATED_NORMAL = 'truncated_normal'\n</code></pre> <p>Truncated Normal distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.LOG_NORMAL","title":"LOG_NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>LOG_NORMAL = 'log_normal'\n</code></pre> <p>Log Normal distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.POISSON","title":"POISSON  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>POISSON = 'poisson'\n</code></pre> <p>Poisson distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.EXPONENTIAL","title":"EXPONENTIAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>EXPONENTIAL = 'exponential'\n</code></pre> <p>Exponential distribution.</p>"},{"location":"reference/stochas/#stochas.DistType.BERNOULLI","title":"BERNOULLI  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>BERNOULLI = 'bernoulli'\n</code></pre> <p>Bernoulli distribution.</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution","title":"ExponentialDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.ExponentialDistribution[ExponentialDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.ExponentialDistribution\n                \n\n\n              click stochas.ExponentialDistribution href \"\" \"stochas.ExponentialDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent the time between events in a Poisson process.</p> <p>In robotics, this is often used to model time-to-failure or the duration between random perturbations.</p> <p>Examples:</p> Python<pre><code># Time in seconds between sensor noise \"glitches\"\ndist = ExponentialDistribution(name=\"glitch_interval\", lam=0.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.lam","title":"lam  <code>instance-attribute</code>","text":"Python<pre><code>lam: float\n</code></pre> <p>Rate parameter (lambda).</p>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.ExponentialDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.LogNormalDistribution","title":"LogNormalDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.LogNormalDistribution[LogNormalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.LogNormalDistribution\n                \n\n\n              click stochas.LogNormalDistribution href \"\" \"stochas.LogNormalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a distribution whose logarithm is normally distributed.</p> <p>Best for variables that are naturally positive and can have \"long-tail\" outliers, such as contact forces or durations.</p> <p>Examples:</p> Python<pre><code># Modeling a scale factor that is usually 1.0 but can occasionally be 5.0\ndist = LogNormalDistribution(name=\"stiffness_scale\", s=0.5, scale=1.0)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.s","title":"s  <code>instance-attribute</code>","text":"Python<pre><code>s: float\n</code></pre> <p>The shape parameter (sigma of the log)</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.scale","title":"scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>scale: float = 1.0\n</code></pre> <p>exp(mu)</p>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.LogNormalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.NormalDistribution","title":"NormalDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.NormalDistribution[NormalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.NormalDistribution\n                \n\n\n              click stochas.NormalDistribution href \"\" \"stochas.NormalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a standard Gaussian \"Bell Curve\" distribution.</p> <p>The Normal distribution is the most common choice for modeling natural variation in physical properties where the value is likely to be near a mean with symmetric falloff.</p> <p>Examples:</p> Python<pre><code># Modeling the mass of a link with 10% standard deviation\ndist = NormalDistribution(name=\"link_mass\", mu=1.5, sigma=0.15)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.NormalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.mu","title":"mu  <code>instance-attribute</code>","text":"Python<pre><code>mu: float\n</code></pre> <p>Mean value of distribution.</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.sigma","title":"sigma  <code>instance-attribute</code>","text":"Python<pre><code>sigma: float\n</code></pre> <p>Standard deviation of distribution. Must be positive.</p>"},{"location":"reference/stochas/#stochas.NormalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.NormalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.PermutationDistribution","title":"PermutationDistribution","text":"<p>               Bases: <code>Distribution[T]</code></p> <pre><code>\n              flowchart TD\n              stochas.PermutationDistribution[PermutationDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.PermutationDistribution\n                \n\n\n              click stochas.PermutationDistribution href \"\" \"stochas.PermutationDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Takes a master list and returns either the original or a shuffled version.</p> <p>Trial 0 (Nominal) returns the items in their provided order. Trial &gt; 0 returns a unique random permutation of those items.</p>"},{"location":"reference/stochas/#stochas.PermutationDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.PermutationDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.PermutationDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.PermutationDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.PermutationDistribution.items","title":"items  <code>instance-attribute</code>","text":"Python<pre><code>items: list[T]\n</code></pre> <p>The master list to be shuffled.</p>"},{"location":"reference/stochas/#stochas.PermutationDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.PermutationDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; ndarray\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; np.ndarray:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.asarray([self.nominal])\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.PermutationDistribution.draw","title":"draw","text":"Python<pre><code>draw(size: int = 1) -&gt; ndarray\n</code></pre> <p>Returns a shuffled version of the items.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def draw(self, size: int = 1) -&gt; np.ndarray:\n    \"\"\"\n    Returns a shuffled version of the items.\n    \"\"\"\n    return np.array([self.rng.permutation(self.items) for _ in range(size)])  # pyright: ignore[reportArgumentType, reportCallIssue]\n</code></pre>"},{"location":"reference/stochas/#stochas.PermutationDistribution.pmf","title":"pmf","text":"Python<pre><code>pmf(x: list[T] | ndarray) -&gt; float\n</code></pre> <p>Uniform probability of 1/n! if x is a valid permutation.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pmf(self, x: list[T] | np.ndarray) -&gt; float:\n    \"\"\"Uniform probability of 1/n! if x is a valid permutation.\"\"\"\n    import math\n\n    n = len(self.items)\n    if len(x) != n or set(x) != set(self.items):\n        return 0.0\n    return 1.0 / math.factorial(n)\n</code></pre>"},{"location":"reference/stochas/#stochas.PoissonDistribution","title":"PoissonDistribution","text":"<p>               Bases: <code>Distribution[int]</code></p> <pre><code>\n              flowchart TD\n              stochas.PoissonDistribution[PoissonDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.PoissonDistribution\n                \n\n\n              click stochas.PoissonDistribution href \"\" \"stochas.PoissonDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent the probability of a number of events occurring in a fixed interval.</p> <p>Useful for counting discrete occurrences, like how many distractor objects should appear in a scene.</p> <p>Examples:</p> Python<pre><code># Sampling the number of random noise impulses to apply during a trial\ndist = PoissonDistribution(name=\"impulse_count\", lam=3.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.lam","title":"lam  <code>instance-attribute</code>","text":"Python<pre><code>lam: float\n</code></pre> <p>Lambda: Average rate of occurrences</p>"},{"location":"reference/stochas/#stochas.PoissonDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.PoissonDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.TriangularDistribution","title":"TriangularDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.TriangularDistribution[TriangularDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.TriangularDistribution\n                \n\n\n              click stochas.TriangularDistribution href \"\" \"stochas.TriangularDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a continuous distribution with a triangular shape.</p> <p>Often used as a \"simpler\" version of the Normal distribution when you know the minimum, maximum, and most likely value (mode).</p> <p>Examples:</p> Python<pre><code># Damping where 1.0 is the spec, but it might range from 0.8 to 1.5\ndist = TriangularDistribution(name=\"joint_damping\", low=0.8, high=1.5, mode=1.0)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: float\n</code></pre> <p>Minimum value of distribution.</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.mode","title":"mode  <code>instance-attribute</code>","text":"Python<pre><code>mode: float\n</code></pre> <p>Peak value of distribution.</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: float\n</code></pre> <p>Maximum value of distribution.</p>"},{"location":"reference/stochas/#stochas.TriangularDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.TriangularDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution","title":"TruncatedNormalDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.TruncatedNormalDistribution[TruncatedNormalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.TruncatedNormalDistribution\n                \n\n\n              click stochas.TruncatedNormalDistribution href \"\" \"stochas.TruncatedNormalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a Normal distribution restricted to a specific interval.</p> <p>Useful for physical parameters that follow a bell curve but have hard physical limits (e.g., a mass that cannot be negative).</p> <p>Examples:</p> Python<pre><code># Friction coefficient with a lower bound of 0 to prevent anti-friction\ndist = TruncatedNormalDistribution(name=\"friction\", mu=0.5, sigma=0.1, low=0.0)\n</code></pre> References <ol> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.mu","title":"mu  <code>instance-attribute</code>","text":"Python<pre><code>mu: float\n</code></pre> <p>Mean value of distribution.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.sigma","title":"sigma  <code>instance-attribute</code>","text":"Python<pre><code>sigma: float\n</code></pre> <p>Standard deviation of distribution.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.low","title":"low  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>low: float = float('-inf')\n</code></pre> <p>Lower bound of distribution.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.high","title":"high  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>high: float = float('inf')\n</code></pre> <p>Upper bound of distribution.</p>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.TruncatedNormalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.UniformDistribution","title":"UniformDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.UniformDistribution[UniformDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.UniformDistribution\n                \n\n\n              click stochas.UniformDistribution href \"\" \"stochas.UniformDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a distribution where every value in a range is equally likely.</p> <p>Use this when you have strict upper and lower bounds but no knowledge of which values are more probable within that window.</p> <p>Examples:</p> Python<pre><code># Randomizing a starting joint position between -0.5 and 0.5 radians\ndist = UniformDistribution(name=\"init_pos\", low=-0.5, high=0.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/#stochas.UniformDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: float\n</code></pre> <p>Minimum value of distribution.</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: float\n</code></pre> <p>Maximum value of distribution.</p>"},{"location":"reference/stochas/#stochas.UniformDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/#stochas.UniformDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValue","title":"NamedValue","text":"<p>               Bases: <code>BaseModel</code>, <code>NumericMixin</code></p> <pre><code>\n              flowchart TD\n              stochas.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n\n                              stochas.mixins.NumericMixin --&gt; stochas.NamedValue\n                \n\n\n              click stochas.NamedValue href \"\" \"stochas.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n            </code></pre> <p>A container for a sampled variable that tracks its initialization state.</p> <p>NamedValues act as the bridge between abstract distributions and concrete simulation parameters. They utilize a state-machine logic to ensure that values are not accidentally overwritten once sampled, and provide a NumericMixin to allow the container to behave like the underlying data in mathematical operations.</p> Notes <p>Once <code>state</code> becomes <code>SET</code>, the <code>value</code> property will raise an error on further assignment attempts unless <code>force_set_value</code> is used.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0sample_dist </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0sample_dist </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> </ul> </li> </ul> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool </li> <li> <code></code>\u00a0DesignCategorical </li> <li> <code></code>\u00a0DesignFloat </li> <li> <code></code>\u00a0DesignInt </li> <li> <code></code>\u00a0NamedValueDict </li> <li> <code></code>\u00a0NamedValueList </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool </li> <li> <code></code>\u00a0DesignCategorical </li> <li> <code></code>\u00a0DesignFloat </li> <li> <code></code>\u00a0DesignInt </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <ul> <li> <code></code>\u00a0NamedValueDict </li> <li> <code></code>\u00a0NamedValueList </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.NamedValue.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/#stochas.NamedValue.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/#stochas.NamedValue.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/#stochas.NamedValue.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/#stochas.NamedValue.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/#stochas.NamedValue.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValue.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValue.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict","title":"NamedValueDict","text":"<p>               Bases: <code>BaseDict[NamedValue[T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.NamedValueDict[NamedValueDict]\n              stochas.base_collections.BaseDict[BaseDict]\n\n                              stochas.base_collections.BaseDict --&gt; stochas.NamedValueDict\n                \n\n\n              click stochas.NamedValueDict href \"\" \"stochas.NamedValueDict\"\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> <p>Dictionary specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0NamedValueList <code></code>\u00a0to_named_value_dict </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0named </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0named </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueList <code></code>\u00a0to_named_value_dict </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0StochasBase <ul> <li> <code></code>\u00a0named </li> <li> <code></code>\u00a0with_override </li> <li> <code></code>\u00a0with_overrides </li> </ul> </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <ul> <li> <code></code>\u00a0named </li> <li> <code></code>\u00a0with_override </li> <li> <code></code>\u00a0with_overrides </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.NamedValueDict.named_value_list","title":"named_value_list  <code>property</code>","text":"Python<pre><code>named_value_list: NamedValueList\n</code></pre> <p>Converts the NamedValueDict to a NamedValueList.</p>"},{"location":"reference/stochas/#stochas.NamedValueDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.get_value","title":"get_value","text":"Python<pre><code>get_value(name: ValueName | str) -&gt; Any\n</code></pre> <p>Gets the NamedValue value of a key.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def get_value(self, name: ValueName | str) -&gt; Any:\n    \"\"\"Gets the NamedValue value of a key.\"\"\"\n    return self[name].value\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueDict.get_raw_value","title":"get_raw_value","text":"Python<pre><code>get_raw_value(name: ValueName | str) -&gt; Any | None\n</code></pre> <p>Gets the NamedValue raw value. This includes None if the value has not yet been set.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def get_raw_value(self, name: ValueName | str) -&gt; Any | None:\n    \"\"\"Gets the NamedValue raw value. This includes None if the value has not yet been set.\"\"\"\n    return self[name].stored_value\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList","title":"NamedValueList","text":"<p>               Bases: <code>BaseList[NamedValue[T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.NamedValueList[NamedValueList]\n              stochas.base_collections.BaseList[BaseList]\n\n                              stochas.base_collections.BaseList --&gt; stochas.NamedValueList\n                \n\n\n              click stochas.NamedValueList href \"\" \"stochas.NamedValueList\"\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> <p>List specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0NamedValueDict <code></code>\u00a0named_value_list </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueDict <code></code>\u00a0named_value_list </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.NamedValueList.to_named_value_dict","title":"to_named_value_dict  <code>property</code>","text":"Python<pre><code>to_named_value_dict: NamedValueDict\n</code></pre> <p>Converts the NamedValueList to a NamedValueDict.</p>"},{"location":"reference/stochas/#stochas.NamedValueList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/#stochas.NamedValueState","title":"NamedValueState","text":"<p>               Bases: <code>StrEnum</code></p> <pre><code>\n              flowchart TD\n              stochas.NamedValueState[NamedValueState]\n\n              \n\n              click stochas.NamedValueState href \"\" \"stochas.NamedValueState\"\n            </code></pre> <p>Internal state of a NamedValue.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> <li> <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.NamedValueState.UNSET","title":"UNSET  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>UNSET = 'unset'\n</code></pre> <p>The value has been initialized but not yet sampled/populated.</p> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> <li> <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> </ul>"},{"location":"reference/stochas/#stochas.NamedValueState.SET","title":"SET  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>SET = 'set'\n</code></pre> <p>The value has been populated and is effectively frozen.</p>"},{"location":"reference/stochas/#stochas.Val","title":"Val","text":"<p>               Bases: <code>BaseModel</code></p> <pre><code>\n              flowchart TD\n              stochas.Val[Val]\n\n              \n\n              click stochas.Val href \"\" \"stochas.Val\"\n            </code></pre> <p>Defines a reference to a variable name.</p> <p>This acts as a 'pointer' or 'placeholder' within other models. During processing, these references would be replaced by actual values sampled from their corresponding distributions.</p>"},{"location":"reference/stochas/#stochas.Val.ref","title":"ref  <code>instance-attribute</code>","text":"Python<pre><code>ref: ValueName\n</code></pre> <p>The unique identifier for the variable to be resolved.</p>"},{"location":"reference/stochas/base/","title":"base","text":""},{"location":"reference/stochas/base/#stochas.base","title":"base","text":""},{"location":"reference/stochas/base/#stochas.base.StochasBase","title":"StochasBase","text":"<p>               Bases: <code>BaseModel</code></p> <pre><code>\n              flowchart TD\n              stochas.base.StochasBase[StochasBase]\n\n              \n\n              click stochas.base.StochasBase href \"\" \"stochas.base.StochasBase\"\n            </code></pre>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Trial number identified for this instance of the model.</p>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Campaign seed for calculating random numbers.</p>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.design","title":"design  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>design: DesignValueDict = Field(\n    default_factory=DesignValueDict\n)\n</code></pre> <p>Registry of parameters for hyperparameter tuning.</p>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.dists","title":"dists  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>dists: DistributionDict = Field(\n    default_factory=DistributionDict\n)\n</code></pre> <p>Random distributions used to generate the model.</p>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.named","title":"named  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>named: SerializeAsAny[NamedValueDict[NDArray]] = Field(\n    default_factory=NamedValueDict[NDArray]\n)\n</code></pre> <p>Final 'baked' values from a random draw, global override, or design study.</p>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.sample_dist","title":"sample_dist","text":"Python<pre><code>sample_dist(\n    dist: Dist,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray]\n</code></pre> <p>Sets the seed and trial number of the distribution, sample, registers it and the sampled value to the model, and returns the named value.</p> <p>If the NamedValue is already registered, the registered named value is returned.</p> <p>Parameters:</p> Name Type Description Default <code>dist</code> <code>Dist</code> <p>Distribution to sample and register.</p> required <code>size</code> <code>int</code> <p>Number of samples to take. Will be embedded in the returned NamedValue. Defaults to 1.</p> <code>1</code> <code>force</code> <code>bool</code> <p>Force the sampled value into the NamedValueDict if it already exists. Defaults to False.</p> <code>False</code> <code>warn</code> <code>bool</code> <p>Whether or not to warn if there is a conflict while forcing. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>NamedValue[NDArray]</code> <p>NamedValue[NDArray]: NamedValue containing the random draw.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def sample_dist(\n    self,\n    dist: Dist,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray]:\n    \"\"\"\n    Sets the seed and trial number of the distribution, sample, registers it and the sampled value to the model, and returns the named value.\n\n    If the NamedValue is already registered, the registered named value is returned.\n\n    Args:\n        dist (Dist): Distribution to sample and register.\n        size (int, optional): Number of samples to take. Will be embedded in the returned NamedValue. Defaults to 1.\n        force (bool, optional): Force the sampled value into the NamedValueDict if it already exists. Defaults to False.\n        warn (bool, optional): Whether or not to warn if there is a conflict while forcing. Defaults to True.\n\n    Returns:\n        NamedValue[NDArray]: NamedValue containing the random draw.\n\n    \"\"\"\n    dist.with_seed(self.seed).with_trial_num(self.trial_num)\n    self.dists.update(dist)\n\n    nv = dist.sample_to_named_value(size=size)\n\n    if nv in self.named and not force:\n        # defined with global override\n        if warn:\n            logger.warning(\n                f\"NamedValue {nv.name} already registered. Returning it instead of the sampled value.\"\n            )\n        return self.named[nv.name]\n    elif nv in self.named and force:\n        # defined with global override but forced to update\n        if warn:\n            logger.warning(\n                f\"NamedValue {nv.name} already registered. Force setting it to the new value in the registry.\",\n            )\n        self.named.force_update(nv, warn=False)\n    else:\n        # standard random draw\n        self.named.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.with_overrides","title":"with_overrides","text":"Python<pre><code>with_overrides(overrides: NamedValueDict[NDArray]) -&gt; Self\n</code></pre> <p>Sets the NamedValueDict to the provided override.</p> <p>This is useful for manually setting some named values to be used.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_overrides(self, overrides: NamedValueDict[NDArray]) -&gt; Self:\n    \"\"\"\n    Sets the NamedValueDict to the provided override.\n\n    This is useful for manually setting some named values to be used.\n    \"\"\"\n    self.named = overrides\n    return self\n</code></pre>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.with_seed","title":"with_seed","text":"Python<pre><code>with_seed(seed: int | None = None) -&gt; Self\n</code></pre> <p>Sets the seed to the provided value.</p> <p>This is useful for initializaing the model.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_seed(self, seed: int | None = None) -&gt; Self:\n    \"\"\"\n    Sets the seed to the provided value.\n\n    This is useful for initializaing the model.\n    \"\"\"\n    self.seed = seed\n    for _, dist in self.dists.items():\n        dist.with_seed(seed)\n    return self\n</code></pre>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.with_trial_num","title":"with_trial_num","text":"Python<pre><code>with_trial_num(trial_num: int) -&gt; Self\n</code></pre> <p>Sets the trial_num to the provided value.</p> <p>This is useful for initializaing the model.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_trial_num(self, trial_num: int) -&gt; Self:\n    \"\"\"\n    Sets the trial_num to the provided value.\n\n    This is useful for initializaing the model.\n    \"\"\"\n    self.trial_num = trial_num\n    for _, dist in self.dists.items():\n        dist.with_trial_num(trial_num)\n    return self\n</code></pre>"},{"location":"reference/stochas/base/#stochas.base.StochasBase.with_override","title":"with_override","text":"Python<pre><code>with_override(override: NamedValueDict[NDArray]) -&gt; Self\n</code></pre> <p>Sets the NamedValueDict to the provided override.</p> <p>This is useful for manually setting some named values to be used.</p> Source code in <code>src/stochas/base.py</code> Python<pre><code>def with_override(self, override: NamedValueDict[NDArray]) -&gt; Self:\n    \"\"\"\n    Sets the NamedValueDict to the provided override.\n\n    This is useful for manually setting some named values to be used.\n    \"\"\"\n    self.named = override\n    return self\n</code></pre>"},{"location":"reference/stochas/base_collections/","title":"base_collections","text":""},{"location":"reference/stochas/base_collections/#stochas.base_collections","title":"base_collections","text":""},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict","title":"BaseDict","text":"<p>               Bases: <code>RootModel[dict[str, T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.base_collections.BaseDict[BaseDict]\n\n              \n\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueDict </li> <li> <code></code>\u00a0DistributionDict </li> <li> <code></code>\u00a0NamedValueDict </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueDict </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionDict </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueDict </li> </ul> </li> </ul>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.__contains__","title":"__contains__","text":"Python<pre><code>__contains__(key: object) -&gt; bool\n</code></pre> <p>Specifies if a key already exists in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __contains__(self, key: object) -&gt; bool:\n    \"\"\"Specifies if a key already exists in the dictionary.\"\"\"\n    return self.root.__contains__(key)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList","title":"BaseList","text":"<p>               Bases: <code>RootModel[list[T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.base_collections.BaseList[BaseList]\n\n              \n\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueList </li> <li> <code></code>\u00a0DistributionList </li> <li> <code></code>\u00a0NamedValueList </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueList </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionList </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueList </li> </ul> </li> </ul>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/base_collections/#stochas.base_collections.BaseList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/design_variable/","title":"design_variable","text":""},{"location":"reference/stochas/design_variable/#stochas.design_variable","title":"design_variable","text":""},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool","title":"DesignBool","text":"<p>               Bases: <code>NamedValue[bool]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.design_variable.DesignBool[DesignBool]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.design_variable.DesignBool\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.design_variable.DesignBool\n                \n\n\n              click stochas.design_variable.DesignBool href \"\" \"stochas.design_variable.DesignBool\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A specific 0/1 toggle, useful for enabling/disabling branches.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignBool.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical","title":"DesignCategorical","text":"<p>               Bases: <code>NamedValue[T]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.design_variable.DesignCategorical[DesignCategorical]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.design_variable.DesignCategorical\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.design_variable.DesignCategorical\n                \n\n\n              click stochas.design_variable.DesignCategorical href \"\" \"stochas.design_variable.DesignCategorical\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A NamedValue that represents a categorical design parameter to be optimized.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.choices","title":"choices  <code>instance-attribute</code>","text":"Python<pre><code>choices: Sequence[T]\n</code></pre> <p>For categorical search (e.g., choices=['stiff', 'soft']).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignCategorical.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat","title":"DesignFloat","text":"<p>               Bases: <code>NamedValue[float]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.design_variable.DesignFloat[DesignFloat]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.design_variable.DesignFloat\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.design_variable.DesignFloat\n                \n\n\n              click stochas.design_variable.DesignFloat href \"\" \"stochas.design_variable.DesignFloat\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A NamedValue that represents a floating point design parameter to be optimized.</p> <p>Contains bounds required for search space discovery.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: float\n</code></pre> <p>Lower bound for hyperparameter tuning.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: float\n</code></pre> <p>Upper bound for hyperparameter tuning.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.log","title":"log  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>log: bool = False\n</code></pre> <p>Use support for log-scale search (useful for learning rates or stiffness).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.step","title":"step  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>step: float | None = None\n</code></pre> <p>Discretize the search space (e.g., step=0.5).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignFloat.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt","title":"DesignInt","text":"<p>               Bases: <code>NamedValue[int]</code>, <code>OptimizationSuggestor</code></p> <pre><code>\n              flowchart TD\n              stochas.design_variable.DesignInt[DesignInt]\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n              stochas.design_variable.OptimizationSuggestor[OptimizationSuggestor]\n\n                              stochas.named_value.NamedValue --&gt; stochas.design_variable.DesignInt\n                                stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n                stochas.design_variable.OptimizationSuggestor --&gt; stochas.design_variable.DesignInt\n                \n\n\n              click stochas.design_variable.DesignInt href \"\" \"stochas.design_variable.DesignInt\"\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n              click stochas.design_variable.OptimizationSuggestor href \"\" \"stochas.design_variable.OptimizationSuggestor\"\n            </code></pre> <p>A NamedValue representing an integer design parameter (e.g., number of solver iterations).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: int\n</code></pre> <p>Lower bound (inclusive).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: int\n</code></pre> <p>Upper bound (inclusive).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.step","title":"step  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>step: int = 1\n</code></pre> <p>Spacing between values.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.log","title":"log  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>log: bool = False\n</code></pre> <p>Whether to sample from a log-scale (useful for orders of magnitude).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignInt.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict","title":"DesignValueDict","text":"<p>               Bases: <code>BaseDict[AnyDesignValue]</code></p> <pre><code>\n              flowchart TD\n              stochas.design_variable.DesignValueDict[DesignValueDict]\n              stochas.base_collections.BaseDict[BaseDict]\n\n                              stochas.base_collections.BaseDict --&gt; stochas.design_variable.DesignValueDict\n                \n\n\n              click stochas.design_variable.DesignValueDict href \"\" \"stochas.design_variable.DesignValueDict\"\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> <p>Dictionary specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueList <code></code>\u00a0to_named_value_dict </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0design </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0design </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueList <code></code>\u00a0to_named_value_dict </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0StochasBase <code></code>\u00a0design </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0design </li> </ul> </li> </ul>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.named_value_list","title":"named_value_list  <code>property</code>","text":"Python<pre><code>named_value_list: DesignValueList\n</code></pre> <p>Converts the DesignValueDict to a DesignValueList.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.get_value","title":"get_value","text":"Python<pre><code>get_value(name: ValueName | str) -&gt; Any\n</code></pre> <p>Gets the NamedValue value of a key.</p> Source code in <code>src/stochas/design_variable.py</code> Python<pre><code>def get_value(self, name: ValueName | str) -&gt; Any:\n    \"\"\"Gets the NamedValue value of a key.\"\"\"\n    return self[name].value\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.get_raw_value","title":"get_raw_value","text":"Python<pre><code>get_raw_value(name: ValueName | str) -&gt; Any | None\n</code></pre> <p>Gets the NamedValue raw value. This includes None if the value has not yet been set.</p> Source code in <code>src/stochas/design_variable.py</code> Python<pre><code>def get_raw_value(self, name: ValueName | str) -&gt; Any | None:\n    \"\"\"Gets the NamedValue raw value. This includes None if the value has not yet been set.\"\"\"\n    return self[name].stored_value\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList","title":"DesignValueList","text":"<p>               Bases: <code>BaseList[AnyDesignValue]</code></p> <pre><code>\n              flowchart TD\n              stochas.design_variable.DesignValueList[DesignValueList]\n              stochas.base_collections.BaseList[BaseList]\n\n                              stochas.base_collections.BaseList --&gt; stochas.design_variable.DesignValueList\n                \n\n\n              click stochas.design_variable.DesignValueList href \"\" \"stochas.design_variable.DesignValueList\"\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> <p>List specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0DesignValueDict <code></code>\u00a0named_value_list </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueDict <code></code>\u00a0named_value_list </li> </ul> </li> </ul>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.to_named_value_dict","title":"to_named_value_dict  <code>property</code>","text":"Python<pre><code>to_named_value_dict: DesignValueDict\n</code></pre> <p>Converts the DesignValueList to a DesignValueDict.</p>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/design_variable/#stochas.design_variable.DesignValueList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/distribution/","title":"distribution","text":""},{"location":"reference/stochas/distribution/#stochas.distribution","title":"distribution","text":"Referenced by: <ul> <li> User Guides Distributions: Defining the Rules of Chance Supported Distribution Types </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistName","title":"DistName  <code>module-attribute</code>","text":"Python<pre><code>DistName = NewType('DistName', str)\n</code></pre> <p>Alias of string. Used to type hint a distribution.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0name </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0name </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0name </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0name </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0name </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.NOMINAL_TRIAL_NUM","title":"NOMINAL_TRIAL_NUM  <code>module-attribute</code>","text":"Python<pre><code>NOMINAL_TRIAL_NUM = 0\n</code></pre> <p>Trial number definition where nominal case will be used.</p> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0trial_num </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0trial_num </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0trial_num </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0trial_num </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.UNDEFINED","title":"UNDEFINED  <code>module-attribute</code>","text":"Python<pre><code>UNDEFINED = UNDEFINED\n</code></pre> <p>Sentinel to differentiate between None and unset.</p> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0nominal </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0nominal </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0nominal </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType","title":"DistType","text":"<p>               Bases: <code>StrEnum</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.DistType[DistType]\n\n              \n\n              click stochas.distribution.DistType href \"\" \"stochas.distribution.DistType\"\n            </code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.NORMAL","title":"NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>NORMAL = 'normal'\n</code></pre> <p>Normal distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.UNIFORM","title":"UNIFORM  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>UNIFORM = 'uniform'\n</code></pre> <p>Uniform distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.DISCRETE_UNIFORM","title":"DISCRETE_UNIFORM  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>DISCRETE_UNIFORM = 'discrete_uniform'\n</code></pre> <p>Discrete uniform distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.CATEGORICAL","title":"CATEGORICAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>CATEGORICAL = 'categorical'\n</code></pre> <p>Categorical distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.PERMUTATION","title":"PERMUTATION  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>PERMUTATION = 'permutation'\n</code></pre> <p>Permutation distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.TRIANGULAR","title":"TRIANGULAR  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>TRIANGULAR = 'triangular'\n</code></pre> <p>Triangular distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.TRUNCATED_NORMAL","title":"TRUNCATED_NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>TRUNCATED_NORMAL = 'truncated_normal'\n</code></pre> <p>Truncated Normal distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.LOG_NORMAL","title":"LOG_NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>LOG_NORMAL = 'log_normal'\n</code></pre> <p>Log Normal distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.POISSON","title":"POISSON  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>POISSON = 'poisson'\n</code></pre> <p>Poisson distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.EXPONENTIAL","title":"EXPONENTIAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>EXPONENTIAL = 'exponential'\n</code></pre> <p>Exponential distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistType.BERNOULLI","title":"BERNOULLI  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>BERNOULLI = 'bernoulli'\n</code></pre> <p>Bernoulli distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Undefined","title":"Undefined","text":"<p>               Bases: <code>StrEnum</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.Undefined[Undefined]\n\n              \n\n              click stochas.distribution.Undefined href \"\" \"stochas.distribution.Undefined\"\n            </code></pre> <p>Undefined value.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution","title":"Distribution","text":"<p>               Bases: <code>BaseModel</code>, <code>ABC</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.Distribution[Distribution]\n\n              \n\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution </li> <li> <code></code>\u00a0CategoricalDistribution </li> <li> <code></code>\u00a0DiscreteUniformDistribution </li> <li> <code></code>\u00a0ExponentialDistribution </li> <li> <code></code>\u00a0LogNormalDistribution </li> <li> <code></code>\u00a0NormalDistribution </li> <li> <code></code>\u00a0PermutationDistribution </li> <li> <code></code>\u00a0PoissonDistribution </li> <li> <code></code>\u00a0TriangularDistribution </li> <li> <code></code>\u00a0TruncatedNormalDistribution </li> <li> <code></code>\u00a0UniformDistribution </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution </li> <li> <code></code>\u00a0CategoricalDistribution </li> <li> <code></code>\u00a0DiscreteUniformDistribution </li> <li> <code></code>\u00a0ExponentialDistribution </li> <li> <code></code>\u00a0LogNormalDistribution </li> <li> <code></code>\u00a0NormalDistribution </li> <li> <code></code>\u00a0PermutationDistribution </li> <li> <code></code>\u00a0PoissonDistribution </li> <li> <code></code>\u00a0TriangularDistribution </li> <li> <code></code>\u00a0TruncatedNormalDistribution </li> <li> <code></code>\u00a0UniformDistribution </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.is_continuous","title":"is_continuous  <code>abstractmethod</code> <code>property</code>","text":"Python<pre><code>is_continuous: bool\n</code></pre> <p>Flag indicating if the distribution is continuous (True) or discrete (False).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.draw","title":"draw  <code>abstractmethod</code>","text":"Python<pre><code>draw(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>Perform a random draw</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef draw(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"Perform a random draw\"\"\"\n    msg = f\"This method has not been implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.pdf","title":"pdf  <code>abstractmethod</code>","text":"Python<pre><code>pdf(x: float | ndarray) -&gt; float | ndarray\n</code></pre> <p>Probability Density Function.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef pdf(self, x: float | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"Probability Density Function.\"\"\"\n    msg = f\"This method has not been implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.cdf","title":"cdf  <code>abstractmethod</code>","text":"Python<pre><code>cdf(x: float | ndarray) -&gt; float | ndarray\n</code></pre> <p>Cumulative Distribution Function.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef cdf(self, x: float | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"Cumulative Distribution Function.\"\"\"\n    msg = f\"This method has not been implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.Distribution.ppf","title":"ppf  <code>abstractmethod</code>","text":"Python<pre><code>ppf(q: float | ndarray) -&gt; float | ndarray\n</code></pre> <p>Percent Point Function (Inverse of CDF). Used to find the value at a specific quantile (e.g., 0.95).</p> <p>Parameters:</p> Name Type Description Default <code>q</code> <code>float | ndarray</code> <p>Probability (0.0 to 1.0).</p> required Source code in <code>src/stochas/distribution.py</code> Python<pre><code>@abstractmethod\ndef ppf(self, q: float | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"\n    Percent Point Function (Inverse of CDF). Used to find the value at a specific quantile (e.g., 0.95).\n\n    Args:\n        q: Probability (0.0 to 1.0).\n\n    \"\"\"\n    msg = f\"Method 'ppf' not implemented for {self.__class__.__name__}\"\n    logger.error(msg)\n    raise NotImplementedError(msg)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution","title":"NormalDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.NormalDistribution[NormalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.NormalDistribution\n                \n\n\n              click stochas.distribution.NormalDistribution href \"\" \"stochas.distribution.NormalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a standard Gaussian \"Bell Curve\" distribution.</p> <p>The Normal distribution is the most common choice for modeling natural variation in physical properties where the value is likely to be near a mean with symmetric falloff.</p> <p>Examples:</p> Python<pre><code># Modeling the mass of a link with 10% standard deviation\ndist = NormalDistribution(name=\"link_mass\", mu=1.5, sigma=0.15)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.mu","title":"mu  <code>instance-attribute</code>","text":"Python<pre><code>mu: float\n</code></pre> <p>Mean value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.sigma","title":"sigma  <code>instance-attribute</code>","text":"Python<pre><code>sigma: float\n</code></pre> <p>Standard deviation of distribution. Must be positive.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.NormalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution","title":"UniformDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.UniformDistribution[UniformDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.UniformDistribution\n                \n\n\n              click stochas.distribution.UniformDistribution href \"\" \"stochas.distribution.UniformDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a distribution where every value in a range is equally likely.</p> <p>Use this when you have strict upper and lower bounds but no knowledge of which values are more probable within that window.</p> <p>Examples:</p> Python<pre><code># Randomizing a starting joint position between -0.5 and 0.5 radians\ndist = UniformDistribution(name=\"init_pos\", low=-0.5, high=0.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: float\n</code></pre> <p>Minimum value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: float\n</code></pre> <p>Maximum value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.UniformDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution","title":"DiscreteUniformDistribution","text":"<p>               Bases: <code>Distribution[int]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.DiscreteUniformDistribution[DiscreteUniformDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.DiscreteUniformDistribution\n                \n\n\n              click stochas.distribution.DiscreteUniformDistribution href \"\" \"stochas.distribution.DiscreteUniformDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a distribution where every integer in a range is equally likely.</p> <p>Use this for discrete counts like number of objects, gear teeth, or array indices where you need equal probability across the whole range.</p> <p>Examples:</p> Python<pre><code>dist = DiscreteUniformDistribution(name=name, low=-1, high=2)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.pmf","title":"pmf","text":"Python<pre><code>pmf(k: int | ndarray) -&gt; float | ndarray\n</code></pre> <p>Probability Mass Function.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pmf(self, k: int | np.ndarray) -&gt; float | np.ndarray:\n    \"\"\"Probability Mass Function.\"\"\"\n    return self._scipy.pmf(k)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DiscreteUniformDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution","title":"CategoricalDistribution","text":"<p>               Bases: <code>Distribution[T]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.CategoricalDistribution[CategoricalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.CategoricalDistribution\n                \n\n\n              click stochas.distribution.CategoricalDistribution href \"\" \"stochas.distribution.CategoricalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a discrete distribution over a fixed set of named choices.</p> <p>Ideal for switching between discrete simulation states, such as different materials, object types, or floor textures.</p> <p>Examples:</p> Python<pre><code>class Material(StrEnum):\n    STEEL = \"steel\"\n    WOOD = \"wood\"\n\ndist = CategoricalDistribution[Material](\n    name=\"floor_material\",\n    choices={Material.STEEL: 0.8, Material.WOOD: 0.2}\n)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.choices","title":"choices  <code>instance-attribute</code>","text":"Python<pre><code>choices: dict[T, float]\n</code></pre> <p>Choices for the categorical distribution. Tuples have the format (category, probability). This guarantees each category has an associated probability.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.pdf","title":"pdf","text":"Python<pre><code>pdf(x: Any) -&gt; float | NDArray[Any, float]\n</code></pre> <p>Categorical distributions have no PDF. Did you mean to use pmf?</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pdf(self, x: Any) -&gt; float | NDArray[Any, float]:\n    \"\"\"Categorical distributions have no PDF. Did you mean to use pmf?\"\"\"\n    logger.warning(DISCRETE_MSG)\n    return self.pmf(x=x)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.pmf","title":"pmf","text":"Python<pre><code>pmf(x: T) -&gt; float\n</code></pre> <p>Probability Mass Function. Returns the probability of a specific category 'x'.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pmf(self, x: T) -&gt; float:\n    \"\"\"\n    Probability Mass Function. Returns the probability of a specific category 'x'.\n    \"\"\"\n    try:\n        return self.choices[x]\n    except KeyError:\n        return 0.0\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.cdf","title":"cdf","text":"Python<pre><code>cdf(x: Any) -&gt; float\n</code></pre> <p>Cumulative Distribution Function.</p> Note <p>This follows the order of the 'choices' list.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def cdf(self, x: Any) -&gt; float:\n    \"\"\"\n    Cumulative Distribution Function.\n\n    Note:\n        This follows the order of the 'choices' list.\n\n    \"\"\"\n    try:\n        idx = self.categories.index(x)\n        return self._scipy.cdf(idx)\n    except ValueError:\n        return 0.0\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.CategoricalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution","title":"PermutationDistribution","text":"<p>               Bases: <code>Distribution[T]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.PermutationDistribution[PermutationDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.PermutationDistribution\n                \n\n\n              click stochas.distribution.PermutationDistribution href \"\" \"stochas.distribution.PermutationDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Takes a master list and returns either the original or a shuffled version.</p> <p>Trial 0 (Nominal) returns the items in their provided order. Trial &gt; 0 returns a unique random permutation of those items.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.items","title":"items  <code>instance-attribute</code>","text":"Python<pre><code>items: list[T]\n</code></pre> <p>The master list to be shuffled.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; ndarray\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; np.ndarray:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.asarray([self.nominal])\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.draw","title":"draw","text":"Python<pre><code>draw(size: int = 1) -&gt; ndarray\n</code></pre> <p>Returns a shuffled version of the items.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def draw(self, size: int = 1) -&gt; np.ndarray:\n    \"\"\"\n    Returns a shuffled version of the items.\n    \"\"\"\n    return np.array([self.rng.permutation(self.items) for _ in range(size)])  # pyright: ignore[reportArgumentType, reportCallIssue]\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.pmf","title":"pmf","text":"Python<pre><code>pmf(x: list[T] | ndarray) -&gt; float\n</code></pre> <p>Uniform probability of 1/n! if x is a valid permutation.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def pmf(self, x: list[T] | np.ndarray) -&gt; float:\n    \"\"\"Uniform probability of 1/n! if x is a valid permutation.\"\"\"\n    import math\n\n    n = len(self.items)\n    if len(x) != n or set(x) != set(self.items):\n        return 0.0\n    return 1.0 / math.factorial(n)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.PermutationDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution","title":"TriangularDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.TriangularDistribution[TriangularDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.TriangularDistribution\n                \n\n\n              click stochas.distribution.TriangularDistribution href \"\" \"stochas.distribution.TriangularDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a continuous distribution with a triangular shape.</p> <p>Often used as a \"simpler\" version of the Normal distribution when you know the minimum, maximum, and most likely value (mode).</p> <p>Examples:</p> Python<pre><code># Damping where 1.0 is the spec, but it might range from 0.8 to 1.5\ndist = TriangularDistribution(name=\"joint_damping\", low=0.8, high=1.5, mode=1.0)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.low","title":"low  <code>instance-attribute</code>","text":"Python<pre><code>low: float\n</code></pre> <p>Minimum value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.mode","title":"mode  <code>instance-attribute</code>","text":"Python<pre><code>mode: float\n</code></pre> <p>Peak value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.high","title":"high  <code>instance-attribute</code>","text":"Python<pre><code>high: float\n</code></pre> <p>Maximum value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.TriangularDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution","title":"TruncatedNormalDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.TruncatedNormalDistribution[TruncatedNormalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.TruncatedNormalDistribution\n                \n\n\n              click stochas.distribution.TruncatedNormalDistribution href \"\" \"stochas.distribution.TruncatedNormalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a Normal distribution restricted to a specific interval.</p> <p>Useful for physical parameters that follow a bell curve but have hard physical limits (e.g., a mass that cannot be negative).</p> <p>Examples:</p> Python<pre><code># Friction coefficient with a lower bound of 0 to prevent anti-friction\ndist = TruncatedNormalDistribution(name=\"friction\", mu=0.5, sigma=0.1, low=0.0)\n</code></pre> References <ol> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.mu","title":"mu  <code>instance-attribute</code>","text":"Python<pre><code>mu: float\n</code></pre> <p>Mean value of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.sigma","title":"sigma  <code>instance-attribute</code>","text":"Python<pre><code>sigma: float\n</code></pre> <p>Standard deviation of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.low","title":"low  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>low: float = float('-inf')\n</code></pre> <p>Lower bound of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.high","title":"high  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>high: float = float('inf')\n</code></pre> <p>Upper bound of distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.TruncatedNormalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution","title":"LogNormalDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.LogNormalDistribution[LogNormalDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.LogNormalDistribution\n                \n\n\n              click stochas.distribution.LogNormalDistribution href \"\" \"stochas.distribution.LogNormalDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a distribution whose logarithm is normally distributed.</p> <p>Best for variables that are naturally positive and can have \"long-tail\" outliers, such as contact forces or durations.</p> <p>Examples:</p> Python<pre><code># Modeling a scale factor that is usually 1.0 but can occasionally be 5.0\ndist = LogNormalDistribution(name=\"stiffness_scale\", s=0.5, scale=1.0)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.s","title":"s  <code>instance-attribute</code>","text":"Python<pre><code>s: float\n</code></pre> <p>The shape parameter (sigma of the log)</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.scale","title":"scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>scale: float = 1.0\n</code></pre> <p>exp(mu)</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.LogNormalDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution","title":"PoissonDistribution","text":"<p>               Bases: <code>Distribution[int]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.PoissonDistribution[PoissonDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.PoissonDistribution\n                \n\n\n              click stochas.distribution.PoissonDistribution href \"\" \"stochas.distribution.PoissonDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent the probability of a number of events occurring in a fixed interval.</p> <p>Useful for counting discrete occurrences, like how many distractor objects should appear in a scene.</p> <p>Examples:</p> Python<pre><code># Sampling the number of random noise impulses to apply during a trial\ndist = PoissonDistribution(name=\"impulse_count\", lam=3.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.lam","title":"lam  <code>instance-attribute</code>","text":"Python<pre><code>lam: float\n</code></pre> <p>Lambda: Average rate of occurrences</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.PoissonDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution","title":"ExponentialDistribution","text":"<p>               Bases: <code>Distribution[float]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.ExponentialDistribution[ExponentialDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.ExponentialDistribution\n                \n\n\n              click stochas.distribution.ExponentialDistribution href \"\" \"stochas.distribution.ExponentialDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent the time between events in a Poisson process.</p> <p>In robotics, this is often used to model time-to-failure or the duration between random perturbations.</p> <p>Examples:</p> Python<pre><code># Time in seconds between sensor noise \"glitches\"\ndist = ExponentialDistribution(name=\"glitch_interval\", lam=0.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.lam","title":"lam  <code>instance-attribute</code>","text":"Python<pre><code>lam: float\n</code></pre> <p>Rate parameter (lambda).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.ExponentialDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution","title":"BernoulliDistribution","text":"<p>               Bases: <code>Distribution[bool]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.BernoulliDistribution[BernoulliDistribution]\n              stochas.distribution.Distribution[Distribution]\n\n                              stochas.distribution.Distribution --&gt; stochas.distribution.BernoulliDistribution\n                \n\n\n              click stochas.distribution.BernoulliDistribution href \"\" \"stochas.distribution.BernoulliDistribution\"\n              click stochas.distribution.Distribution href \"\" \"stochas.distribution.Distribution\"\n            </code></pre> <p>Represent a single binary trial (Success/Failure).</p> <p>The simplest distribution, used for \"on/off\" flags, such as enabling or disabling a specific sensor or randomized model feature.</p> <p>Examples:</p> Python<pre><code># 50% chance to enable gravity compensation for this trial\ndist = BernoulliDistribution(name=\"use_grav_comp\", p=0.5)\n</code></pre> References <ol> <li>NumPy</li> <li>SciPy</li> <li>Wikipedia</li> </ol> Note <p></p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.p","title":"p  <code>instance-attribute</code>","text":"Python<pre><code>p: float\n</code></pre> <p>Probability of success (0.0 to 1.0).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: DistName\n</code></pre> <p>Name of the distribution.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>seed: int | None = None\n</code></pre> <p>Seed of the distribution.</p> <p>Leave as None or omit to use a random seed. If the seed is not None, the specified seed will be salted with the name and trial_num attributes to add randomness. This allows you to use the same seed for multiple distributions while also being able to simply serialize and deserialize the distribution. See the <code>validate_seed</code> validator method for how the seed is hashed.</p> Note Salt Functional Purpose Description <code>seed</code> Campaign Entropy Controls the broad \"global\" cycle. Change this to generate a completely new set of results for the same analysis (e.g., using a date-based seed for a daily batch). <code>name</code> Parameter Decoupling Ensures unique draws for different parameters. Without this, two distributions with the same config (e.g., an <code>x</code> and <code>y</code> center of gravity) would produce identical, coupled values. <code>trial_num</code> Iteration Variance Provides a unique draw for each Monte Carlo trial. This ensures that every individual simulation trial receives a unique value from the dispersion. <p>Clear as mud?</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.nominal","title":"nominal  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>nominal: T | None | SerializableUndefined = Field(\n    default=UNDEFINED\n)\n</code></pre> <p>Value the distribution should take if the trial_number attribute is equal to 0.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.trial_num","title":"trial_num  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>trial_num: int = NOMINAL_TRIAL_NUM\n</code></pre> <p>Run number for sampling from the distribution. This is used to salt the seed (if specified).</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.rng","title":"rng  <code>property</code>","text":"Python<pre><code>rng: Generator\n</code></pre> <p>Provides a localized random number generator.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.sample","title":"sample","text":"Python<pre><code>sample(size: int = 1) -&gt; NDArray[Any, T]\n</code></pre> <p>The core sampling logic for the distribution.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample(self, size: int = 1) -&gt; NDArray[Any, T]:\n    \"\"\"The core sampling logic for the distribution.\"\"\"\n    if self.is_nominal and self.nominal is not UNDEFINED:\n        return np.full(size, self.nominal)\n    else:\n        return self.draw(size=size)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.BernoulliDistribution.sample_and_update_dicts","title":"sample_and_update_dicts","text":"Python<pre><code>sample_and_update_dicts(\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]\n</code></pre> <p>Samples from the distribution and registers the result.</p> Source code in <code>src/stochas/distribution.py</code> Python<pre><code>def sample_and_update_dicts(\n    self,\n    dist_dict: DistributionDict,\n    named_value_dict: NamedValueDict,\n    size: int = 1,\n    force: bool = False,\n    warn: bool = True,\n) -&gt; NamedValue[NDArray[Any, T]]:\n    \"\"\"Samples from the distribution and registers the result.\"\"\"\n    if self.name in named_value_dict and not force:\n        return named_value_dict[self.name]\n    elif self.name in named_value_dict and force and warn:\n        logger.warning(\n            f\"NamedValue {self.name} already exists in named_value_list. Overwriting!\"\n        )\n\n    nv = self.sample_to_named_value(size=size)\n\n    dist_dict.update(self)  # pyright: ignore[reportArgumentType]\n    if force:\n        named_value_dict.force_update(nv, warn=warn)\n    else:\n        named_value_dict.update(nv)\n    return nv\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict","title":"DistributionDict","text":"<p>               Bases: <code>BaseDict[Dist]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.DistributionDict[DistributionDict]\n              stochas.base_collections.BaseDict[BaseDict]\n\n                              stochas.base_collections.BaseDict --&gt; stochas.distribution.DistributionDict\n                \n\n\n              click stochas.distribution.DistributionDict href \"\" \"stochas.distribution.DistributionDict\"\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> <p>Dictionary specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DistributionList <code></code>\u00a0to_distribution_dict </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionList <code></code>\u00a0to_distribution_dict </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0dists </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.distribution_list","title":"distribution_list  <code>property</code>","text":"Python<pre><code>distribution_list: DistributionList\n</code></pre> <p>Converts the DistributionDict to a DistributionList.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.__contains__","title":"__contains__","text":"Python<pre><code>__contains__(key: object) -&gt; bool\n</code></pre> <p>Specifies if a key already exists in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __contains__(self, key: object) -&gt; bool:\n    \"\"\"Specifies if a key already exists in the dictionary.\"\"\"\n    return self.root.__contains__(key)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList","title":"DistributionList","text":"<p>               Bases: <code>BaseList[Dist]</code></p> <pre><code>\n              flowchart TD\n              stochas.distribution.DistributionList[DistributionList]\n              stochas.base_collections.BaseList[BaseList]\n\n                              stochas.base_collections.BaseList --&gt; stochas.distribution.DistributionList\n                \n\n\n              click stochas.distribution.DistributionList href \"\" \"stochas.distribution.DistributionList\"\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> <p>List specifically for distributions.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0DistributionDict <code></code>\u00a0distribution_list </li> <li> distribution <code></code>\u00a0distribution <code></code>\u00a0DistributionDict <code></code>\u00a0distribution_list </li> </ul> </li> </ul>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.to_distribution_dict","title":"to_distribution_dict  <code>property</code>","text":"Python<pre><code>to_distribution_dict: DistributionDict\n</code></pre> <p>Converts the DistributionList to a DistributionDict.</p>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/distribution/#stochas.distribution.DistributionList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"reference/stochas/mixins/","title":"mixins","text":""},{"location":"reference/stochas/mixins/#stochas.mixins","title":"mixins","text":"<p>Defines numeric mixins so that classes with a value attribute can act like a number.</p>"},{"location":"reference/stochas/mixins/#stochas.mixins.HasValue","title":"HasValue","text":"<p>               Bases: <code>Protocol</code></p> <pre><code>\n              flowchart TD\n              stochas.mixins.HasValue[HasValue]\n\n              \n\n              click stochas.mixins.HasValue href \"\" \"stochas.mixins.HasValue\"\n            </code></pre> <p>The contract: Any class using the mixin must have a .value property.</p>"},{"location":"reference/stochas/mixins/#stochas.mixins.NumericMixin","title":"NumericMixin","text":"<p>A mixin that proxies operations to the .value property.</p> <p>This allows a NamedValue to behave like a float, int, str, or NDArray.</p> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> \u00a0stochas \u00a0NamedValue </li> <li> named_value \u00a0named_value \u00a0NamedValue </li> </ul> </li> </ul>"},{"location":"reference/stochas/mixins/#stochas.mixins.NumericMixin.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/named_value/","title":"named_value","text":""},{"location":"reference/stochas/named_value/#stochas.named_value","title":"named_value","text":""},{"location":"reference/stochas/named_value/#stochas.named_value.ValueName","title":"ValueName  <code>module-attribute</code>","text":"Python<pre><code>ValueName = NewType('ValueName', str)\n</code></pre> <p>Alias of string. Used to type hint a named value's name.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0name </li> <li> <code></code>\u00a0NamedValue <code></code>\u00a0name </li> <li> <code></code>\u00a0Val <code></code>\u00a0ref </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0name </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0name </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <ul> <li> <code></code>\u00a0NamedValue <code></code>\u00a0name </li> <li> <code></code>\u00a0Val <code></code>\u00a0ref </li> </ul> </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> <li> <code></code>\u00a0NamedValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <code></code>\u00a0DesignValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueDict <ul> <li> <code></code>\u00a0get_raw_value </li> <li> <code></code>\u00a0get_value </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/named_value/#stochas.named_value.Val","title":"Val","text":"<p>               Bases: <code>BaseModel</code></p> <pre><code>\n              flowchart TD\n              stochas.named_value.Val[Val]\n\n              \n\n              click stochas.named_value.Val href \"\" \"stochas.named_value.Val\"\n            </code></pre> <p>Defines a reference to a variable name.</p> <p>This acts as a 'pointer' or 'placeholder' within other models. During processing, these references would be replaced by actual values sampled from their corresponding distributions.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.Val.ref","title":"ref  <code>instance-attribute</code>","text":"Python<pre><code>ref: ValueName\n</code></pre> <p>The unique identifier for the variable to be resolved.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueState","title":"NamedValueState","text":"<p>               Bases: <code>StrEnum</code></p> <pre><code>\n              flowchart TD\n              stochas.named_value.NamedValueState[NamedValueState]\n\n              \n\n              click stochas.named_value.NamedValueState href \"\" \"stochas.named_value.NamedValueState\"\n            </code></pre> <p>Internal state of a NamedValue.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> <li> <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> </ul>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueState.UNSET","title":"UNSET  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>UNSET = 'unset'\n</code></pre> <p>The value has been initialized but not yet sampled/populated.</p> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> <li> <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignCategorical <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignFloat <code></code>\u00a0state </li> <li> <code></code>\u00a0DesignInt <code></code>\u00a0state </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValue <code></code>\u00a0state </li> </ul> </li> </ul>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueState.SET","title":"SET  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>SET = 'set'\n</code></pre> <p>The value has been populated and is effectively frozen.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue","title":"NamedValue","text":"<p>               Bases: <code>BaseModel</code>, <code>NumericMixin</code></p> <pre><code>\n              flowchart TD\n              stochas.named_value.NamedValue[NamedValue]\n              stochas.mixins.NumericMixin[NumericMixin]\n\n                              stochas.mixins.NumericMixin --&gt; stochas.named_value.NamedValue\n                \n\n\n              click stochas.named_value.NamedValue href \"\" \"stochas.named_value.NamedValue\"\n              click stochas.mixins.NumericMixin href \"\" \"stochas.mixins.NumericMixin\"\n            </code></pre> <p>A container for a sampled variable that tracks its initialization state.</p> <p>NamedValues act as the bridge between abstract distributions and concrete simulation parameters. They utilize a state-machine logic to ensure that values are not accidentally overwritten once sampled, and provide a NumericMixin to allow the container to behave like the underlying data in mathematical operations.</p> Notes <p>Once <code>state</code> becomes <code>SET</code>, the <code>value</code> property will raise an error on further assignment attempts unless <code>force_set_value</code> is used.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0sample_dist </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0sample_dist </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> </ul> </li> </ul> Subclassed by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0DesignBool </li> <li> <code></code>\u00a0DesignCategorical </li> <li> <code></code>\u00a0DesignFloat </li> <li> <code></code>\u00a0DesignInt </li> <li> <code></code>\u00a0NamedValueDict </li> <li> <code></code>\u00a0NamedValueList </li> </ul> </li> <li> design_variable <code></code>\u00a0design_variable <ul> <li> <code></code>\u00a0DesignBool </li> <li> <code></code>\u00a0DesignCategorical </li> <li> <code></code>\u00a0DesignFloat </li> <li> <code></code>\u00a0DesignInt </li> </ul> </li> <li> named_value <code></code>\u00a0named_value <ul> <li> <code></code>\u00a0NamedValueDict </li> <li> <code></code>\u00a0NamedValueList </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.name","title":"name  <code>instance-attribute</code>","text":"Python<pre><code>name: ValueName\n</code></pre> <p>The unique identifier for this parameter.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>state: NamedValueState = Field(default=UNSET)\n</code></pre> <p>The current lifecycle state (SET or UNSET).</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.stored_value","title":"stored_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"Python<pre><code>stored_value: Annotated[\n    T | UnsetType, BeforeValidator(_numpy_safe_sentinel)\n] = Field(default=UNSET_SENTINEL)\n</code></pre> <p>The actual data held by this container.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.value","title":"value  <code>property</code> <code>writable</code>","text":"Python<pre><code>value: T\n</code></pre> <p>Returns the stored value if it has been set.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the state is UNSET.</p> <code>RuntimeError</code> <p>If the internal state is corrupted.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.is_set","title":"is_set  <code>property</code>","text":"Python<pre><code>is_set: bool\n</code></pre> <p>True if the value has been populated.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.validate_state","title":"validate_state","text":"Python<pre><code>validate_state() -&gt; Self\n</code></pre> <p>Synchronizes the state enum with the actual stored_value content.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>@model_validator(mode=\"after\")\ndef validate_state(self) -&gt; Self:\n    \"\"\"Synchronizes the state enum with the actual stored_value content.\"\"\"\n    is_sentinel = (\n        isinstance(self.stored_value, str) and self.stored_value == UNSET_SENTINEL\n    )\n\n    match self.state:\n        case NamedValueState.UNSET:\n            if not is_sentinel:\n                self.state = NamedValueState.SET\n        case NamedValueState.SET:\n            if is_sentinel:\n                msg = f\"{self.name} stored value cannot be set to `NamedValueState.UNSET`\"\n                logger.error(msg)\n                raise ValueError(msg)\n        case _:\n            msg = f\"The enumeration for {self.state} has not been implemented.\"\n            logger.error(msg)\n            raise NotImplementedError(msg)\n    return self\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.force_set_value","title":"force_set_value","text":"Python<pre><code>force_set_value(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Manually overrides the stored value regardless of current state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>T</code> <p>The new value to store.</p> required <code>warn</code> <code>bool</code> <p>If True, logs a warning about the override.</p> <code>True</code> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def force_set_value(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"\n    Manually overrides the stored value regardless of current state.\n\n    Args:\n        value: The new value to store.\n        warn: If True, logs a warning about the override.\n\n    \"\"\"\n    if warn:\n        logger.warning(f\"Forcing value of NamedValue {self.name} to {value}\")\n    self.stored_value = value\n    self.state = NamedValueState.SET\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValue.squeeze","title":"squeeze","text":"Python<pre><code>squeeze(axis: int | tuple[int, ...] | None = None) -&gt; Self\n</code></pre> <p>Removes axes of length one from the underlying array.</p> Source code in <code>src/stochas/mixins.py</code> Python<pre><code>def squeeze(self: HasValue, axis: int | tuple[int, ...] | None = None) -&gt; Self:  # pyright: ignore[reportGeneralTypeIssues]\n    \"\"\"\n    Removes axes of length one from the underlying array.\n    \"\"\"\n    # 1. Perform the squeeze on the data\n    squeezed_data = self.value.squeeze(axis=axis)\n\n    # 2. Update the underlying container\n    # We use force_set_value to bypass any \"frozen\" state checks\n    if hasattr(self, \"force_set_value\"):\n        self.force_set_value(squeezed_data, warn=False)\n    else:\n        # Fallback for simple containers\n        self.stored_value = squeezed_data\n\n    return self  # pyright: ignore[reportReturnType]\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict","title":"NamedValueDict","text":"<p>               Bases: <code>BaseDict[NamedValue[T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.named_value.NamedValueDict[NamedValueDict]\n              stochas.base_collections.BaseDict[BaseDict]\n\n                              stochas.base_collections.BaseDict --&gt; stochas.named_value.NamedValueDict\n                \n\n\n              click stochas.named_value.NamedValueDict href \"\" \"stochas.named_value.NamedValueDict\"\n              click stochas.base_collections.BaseDict href \"\" \"stochas.base_collections.BaseDict\"\n            </code></pre> <p>Dictionary specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0NamedValueList <code></code>\u00a0to_named_value_dict </li> <li> <code></code>\u00a0StochasBase <code></code>\u00a0named </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <code></code>\u00a0named </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueList <code></code>\u00a0to_named_value_dict </li> </ul> </li> </ul> Used by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0StochasBase <ul> <li> <code></code>\u00a0named </li> <li> <code></code>\u00a0with_override </li> <li> <code></code>\u00a0with_overrides </li> </ul> </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> <li> base <code></code>\u00a0base <code></code>\u00a0StochasBase <ul> <li> <code></code>\u00a0named </li> <li> <code></code>\u00a0with_override </li> <li> <code></code>\u00a0with_overrides </li> </ul> </li> <li> distribution <code></code>\u00a0distribution <ul> <li> <code></code>\u00a0BernoulliDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0CategoricalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0DiscreteUniformDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0Distribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0ExponentialDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0LogNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0NormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PermutationDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0PoissonDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TriangularDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0TruncatedNormalDistribution <code></code>\u00a0sample_and_update_dicts </li> <li> <code></code>\u00a0UniformDistribution <code></code>\u00a0sample_and_update_dicts </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.named_value_list","title":"named_value_list  <code>property</code>","text":"Python<pre><code>named_value_list: NamedValueList\n</code></pre> <p>Converts the NamedValueDict to a NamedValueList.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.get_value","title":"get_value","text":"Python<pre><code>get_value(name: ValueName | str) -&gt; Any\n</code></pre> <p>Gets the NamedValue value of a key.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def get_value(self, name: ValueName | str) -&gt; Any:\n    \"\"\"Gets the NamedValue value of a key.\"\"\"\n    return self[name].value\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.get_raw_value","title":"get_raw_value","text":"Python<pre><code>get_raw_value(name: ValueName | str) -&gt; Any | None\n</code></pre> <p>Gets the NamedValue raw value. This includes None if the value has not yet been set.</p> Source code in <code>src/stochas/named_value.py</code> Python<pre><code>def get_raw_value(self, name: ValueName | str) -&gt; Any | None:\n    \"\"\"Gets the NamedValue raw value. This includes None if the value has not yet been set.\"\"\"\n    return self[name].stored_value\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(key: str) -&gt; T\n</code></pre> <p>Get an item in the dictionary with the specified key.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get an item in the dictionary with the specified key.\"\"\"\n    if key not in self.root:\n        msg = f\"'{key}' not found.\"\n        logger.error(msg)\n        raise KeyError(msg)\n    return self.root[key]\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(key: str, value: T) -&gt; None\n</code></pre> <p>Set the value of a single key-value pair.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the value of a single key-value pair.\"\"\"\n    name = getattr(value, \"name\", None)\n    if key != name:\n        msg = f\"Key '{key}' must match name '{name}'\"\n        logger.error(msg)\n        raise ValueError(msg)\n    self.update(value)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[str]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[str]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the dictionary.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.keys","title":"keys","text":"Python<pre><code>keys()\n</code></pre> <p>Returns the keys in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def keys(self):\n    \"\"\"Returns the keys in the dictionary.\"\"\"\n    return self.root.keys()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.values","title":"values","text":"Python<pre><code>values()\n</code></pre> <p>Returns the values in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def values(self):\n    \"\"\"Returns the values in the dictionary.\"\"\"\n    return self.root.values()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.items","title":"items","text":"Python<pre><code>items()\n</code></pre> <p>Returns the key-value pairs in the dictionary as tuples.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def items(self):\n    \"\"\"Returns the key-value pairs in the dictionary as tuples.\"\"\"\n    return self.root.items()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.update","title":"update","text":"Python<pre><code>update(value: T) -&gt; None\n</code></pre> <p>Add a new dictionary key value pair. The key cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update(self, value: T) -&gt; None:\n    \"\"\"Add a new dictionary key value pair. The key cannot already exist in the dictionary.\"\"\"\n    name = getattr(value, \"name\", None)\n    if name in self.root:\n        msg = f\"{name} has already been registered.\"\n        logger.error(msg)\n        raise KeyError(msg)\n\n    self.force_update(value=value, warn=False)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.update_many","title":"update_many","text":"Python<pre><code>update_many(values: Iterable[T]) -&gt; None\n</code></pre> <p>Add many new dictionary key value pair. The keys cannot already exist in the dictionary.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def update_many(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Add many new dictionary key value pair. The keys cannot already exist in the dictionary.\"\"\"\n    for value in values:\n        self.update(value=value)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.force_update","title":"force_update","text":"Python<pre><code>force_update(value: T, warn: bool = True) -&gt; None\n</code></pre> <p>Forces a key-value pair into the dictionary. Overwrites existing key if it exists.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update(self, value: T, warn: bool = True) -&gt; None:\n    \"\"\"Forces a key-value pair into the dictionary. Overwrites existing key if it exists.\"\"\"\n    name = getattr(value, \"name\")\n    if warn:\n        logger.warning(f\"Forcing {name} into {self.__class__.__name__}.\")\n    self.root[name] = value\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueDict.force_update_many","title":"force_update_many","text":"Python<pre><code>force_update_many(\n    values: Iterable[T], warn: bool = True\n) -&gt; None\n</code></pre> <p>Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def force_update_many(self, values: Iterable[T], warn: bool = True) -&gt; None:\n    \"\"\"Forces adding many new dictionary key value pair. Overwrites existing keys if they exist.\"\"\"\n    for value in values:\n        self.force_update(value=value, warn=warn)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList","title":"NamedValueList","text":"<p>               Bases: <code>BaseList[NamedValue[T]]</code></p> <pre><code>\n              flowchart TD\n              stochas.named_value.NamedValueList[NamedValueList]\n              stochas.base_collections.BaseList[BaseList]\n\n                              stochas.base_collections.BaseList --&gt; stochas.named_value.NamedValueList\n                \n\n\n              click stochas.named_value.NamedValueList href \"\" \"stochas.named_value.NamedValueList\"\n              click stochas.base_collections.BaseList href \"\" \"stochas.base_collections.BaseList\"\n            </code></pre> <p>List specifically for sampled results.</p> Returned by: <ul> <li> Code Reference stochas <ul> <li> <code></code>\u00a0stochas <code></code>\u00a0NamedValueDict <code></code>\u00a0named_value_list </li> <li> named_value <code></code>\u00a0named_value <code></code>\u00a0NamedValueDict <code></code>\u00a0named_value_list </li> </ul> </li> </ul>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.to_named_value_dict","title":"to_named_value_dict  <code>property</code>","text":"Python<pre><code>to_named_value_dict: NamedValueDict\n</code></pre> <p>Converts the NamedValueList to a NamedValueDict.</p>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.__len__","title":"__len__","text":"Python<pre><code>__len__() -&gt; int\n</code></pre> <p>Returns the number of elements in the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"Returns the number of elements in the list.\"\"\"\n    return self.root.__len__()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.__iter__","title":"__iter__","text":"Python<pre><code>__iter__() -&gt; Iterator[T]\n</code></pre> <p>Create an iterable object.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __iter__(self) -&gt; Iterator[T]:  # pyright: ignore[reportIncompatibleMethodOverride]\n    \"\"\"Create an iterable object.\"\"\"\n    return self.root.__iter__()\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.__getitem__","title":"__getitem__","text":"Python<pre><code>__getitem__(index: int) -&gt; T\n</code></pre>Python<pre><code>__getitem__(index: slice) -&gt; list[T]\n</code></pre> Python<pre><code>__getitem__(index: int | slice) -&gt; T | list[T]\n</code></pre> <p>Gets the items in the specified index/indices.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __getitem__(self, index: int | slice) -&gt; T | list[T]:\n    \"\"\"Gets the items in the specified index/indices.\"\"\"\n    return self.root[index]\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.__setitem__","title":"__setitem__","text":"Python<pre><code>__setitem__(index: int, value: T) -&gt; None\n</code></pre> <p>Set the object at an index.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __setitem__(self, index: int, value: T) -&gt; None:\n    \"\"\"Set the object at an index.\"\"\"\n    self.root[index] = value\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.__delitem__","title":"__delitem__","text":"Python<pre><code>__delitem__(index: int) -&gt; None\n</code></pre> <p>Delete an element from the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def __delitem__(self, index: int) -&gt; None:\n    \"\"\"Delete an element from the list.\"\"\"\n    del self.root[index]\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.append","title":"append","text":"Python<pre><code>append(value: T) -&gt; None\n</code></pre> <p>Append an item to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def append(self, value: T) -&gt; None:\n    \"\"\"Append an item to the list.\"\"\"\n    self.root.append(value)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.extend","title":"extend","text":"Python<pre><code>extend(values: Iterable[T]) -&gt; None\n</code></pre> <p>Append many items to the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def extend(self, values: Iterable[T]) -&gt; None:\n    \"\"\"Append many items to the list.\"\"\"\n    self.root.extend(values)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.pop","title":"pop","text":"Python<pre><code>pop(index: int = -1) -&gt; T\n</code></pre> <p>Remove and return item at index (default last).</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def pop(self, index: int = -1) -&gt; T:\n    \"\"\"Remove and return item at index (default last).\"\"\"\n    return self.root.pop(index)\n</code></pre>"},{"location":"reference/stochas/named_value/#stochas.named_value.NamedValueList.find_by_name","title":"find_by_name","text":"Python<pre><code>find_by_name(name: str) -&gt; T\n</code></pre> <p>Utility to find a specific named value within the list.</p> Source code in <code>src/stochas/base_collections.py</code> Python<pre><code>def find_by_name(self, name: str) -&gt; T:\n    \"\"\"Utility to find a specific named value within the list.\"\"\"\n    for item in self.root:\n        item_name = getattr(item, \"name\", None)\n        if item_name == name:\n            return item\n    msg = f\"Item '{name}' not found in list.\"\n    logger.error(msg)\n    raise KeyError(msg)\n</code></pre>"},{"location":"user-guides/design-values/","title":"Design Values","text":""},{"location":"user-guides/design-values/#design-values-evolving-your-system","title":"Design Values: Evolving Your System","text":"<p>While Distributions represent the uncertainty you can't control (like wind or manufacturing tolerances), Design Values represent the parameters you can control. These are the knobs you turn to find the absolute best version of your system.</p>"},{"location":"user-guides/design-values/#optimization-vs-randomness","title":"Optimization vs. Randomness","text":"<p>In a generic design workflow, we distinguish between two types of variables:</p> Feature Monte Carlo (Distributions) Optimization (Design Values) Goal Assess reliability and error bounds. Find peak performance or trade-offs. Logic Values are drawn from a PDF/PMF. Values are chosen by a search algorithm. Tooling Standard statistical sampling. Optuna (Tuning) or pymoo (Pareto)."},{"location":"user-guides/design-values/#the-search-space-api","title":"The Search Space API","text":"<p>Design values define a \"Search Space\"\u2014a mathematical boundary that an optimizer is allowed to explore. Every design variable in your registry must provide three things:</p> <ol> <li>A Name: To track the variable across trials.</li> <li>A Default: The initial \"best guess\" before the optimizer starts.</li> <li>Bounds: The constraints (low/high or choices) that the solver must respect.</li> </ol>"},{"location":"user-guides/design-values/#supported-variable-types","title":"Supported Variable Types","text":"Type Data Best Used For... <code>DesignFloat</code> \\(x \\in [low, high]\\) Continuous values like stiffness or damping. <code>DesignInt</code> \\(i \\in \\{low, ..., high\\}\\) Discrete counts like solver iterations or parts. <code>DesignBool</code> \\(\\{True, False\\}\\) Enabling/disabling specific logic branches. <code>DesignCategorical</code> \\(\\{'A', 'B', 'C'\\}\\) Picking between distinct materials or styles."},{"location":"user-guides/design-values/#integrating-with-solvers","title":"Integrating with Solvers","text":"<p>One of the primary advantages of this toolkit is its Solver-Agnostic nature. Your model defines the variables once, and the system can automatically translate them into the specific language of different optimization libraries.</p>"},{"location":"user-guides/design-values/#optuna-integration-to_optuna","title":"Optuna Integration (<code>to_optuna</code>)","text":"<p>For single-objective tuning, the variable suggests a value to a trial.</p> <ul> <li>Continuous: Uses Bayesian TPE or CMA-ES sampling.</li> <li>Refinement: The refine method allows the search space to \"shrink\" around the best known solution for more precision.</li> </ul>"},{"location":"user-guides/design-values/#pymoo-integration-to_pymoo","title":"pymoo Integration (<code>to_pymoo</code>)","text":"<p>For multi-objective trade-offs, the variable generates a pymoo.core.variable object.</p> <ul> <li>Population-based: Allows for genetic operators (Crossover and Mutation).</li> <li>Pareto Front: Enables the discovery of the frontier of efficiency.</li> </ul>"},{"location":"user-guides/design-values/#usage-example","title":"Usage Example","text":"<p>The following script demonstrates how to define a generic \"System\" and sample design variables into a registry for optimization.</p> Python<pre><code>import logging\n\nimport numpy as np\nimport optuna\n\nimport stochas\n\n# Setup logging\nlogging.basicConfig(level=logging.INFO)\n\n\nclass SystemModel(stochas.StochasBase):\n    \"\"\"A generic system model that uses design variables.\"\"\"\n\n    pass\n\n\ndef generate_system(model: SystemModel):\n    # 1. Define a continuous float (e.g., structural stiffness)\n    # The optimizer will explore between 50 and 500.\n    stiffness = model.sample_design(\n        stochas.DesignFloat(\n            name=stochas.ValueName(\"k_stiffness\"),\n            low=50.0,\n            high=500.0,\n            stored_value=150.0,  # the initial guess\n        )\n    )\n\n    # 2. Define an integer count (e.g., number of supports)\n    n_supports = model.sample_design(\n        stochas.DesignInt(\n            name=stochas.ValueName(\"n_supports\"), low=2, high=8, stored_value=4\n        )\n    )\n\n    # 3. Define a categorical choice (e.g., material type)\n    material = model.sample_design(\n        stochas.DesignCategorical(\n            name=stochas.ValueName(\"material\"),\n            choices=[\"aluminum\", \"steel\", \"titanium\"],\n            stored_value=\"steel\",\n        )\n    )\n\n    print(\n        f\"Generated System: {material} frame, {n_supports} supports, k={stiffness:.2f}\"\n    )\n\n\n# --- Optimization Workflow ---\n\n# 1. Create a model\nmodel = SystemModel()\n\n# 2. Discovery Pass: Map the design space\ngenerate_system(model)\ndesign_space = model.design\n\n# 3. Translate to Optuna\nstudy = optuna.create_study(direction=\"minimize\")\n\n\ndef objective(trial):\n    # Ask variables to suggest values to Optuna\n    suggestions = {name: dv.to_optuna(trial) for name, dv in design_space.items()}\n\n    # In a real run, you'd apply these 'suggestions' to the model.named registry\n    # and execute your physics/cost simulation here.\n    score = np.random.random()  # Placeholder for simulation result\n    return score\n\n\nstudy.optimize(objective, n_trials=10)\n</code></pre>"},{"location":"user-guides/design-values/#search-space-refinement","title":"Search Space Refinement","text":"<p>When you reach the end of an optimization study, you may find that the \"best\" trial is settled in a specific neighborhood. The <code>refine</code> method automates the \"zoom-in\" process.</p> <p>Given a refinement factor \\(F \\in (0, 1)\\) and the best known value \\(x_{best}\\), the new bounds are calculated as:</p> \\[low_{new} = \\max(low_{old}, x_{best} - \\frac{(high_{old} - low_{old}) \\cdot F}{2})\\] \\[high_{new} = \\min(high_{old}, x_{best} + \\frac{(high_{old} - low_{old}) \\cdot F}{2})\\] <p>Success</p> <p>By using the <code>sample_design</code> pattern, you have decoupled your Design Intent from the Optimization Math. You can switch from Optuna's Bayesian search to pymoo's Genetic algorithms without changing a single line of your model generation logic.</p>"},{"location":"user-guides/distributions/","title":"Distributions","text":""},{"location":"user-guides/distributions/#distributions-defining-the-rules-of-chance","title":"Distributions: Defining the Rules of Chance","text":"<p>A Distribution is a mathematical recipe for generating random numbers. While a <code>NamedValue</code> represents a single point in time, a <code>Distribution</code> represents the \"shape\" of all possible values.</p>"},{"location":"user-guides/distributions/#the-what-and-the-why","title":"The \"What\" and the \"Why\"","text":"<p>Simulations are often used for Monte Carlo analysis\u2014running the same task hundreds of times with slight variations to see how often it fails. Distributions define those variations.</p> <p>Instead of saying \"the floor is slippery,\" you define a <code>UniformDistribution</code> for friction between 0.1 and 0.4. The system will then pick a new, valid number for every trial.</p>"},{"location":"user-guides/distributions/#advanced-features","title":"Advanced Features","text":""},{"location":"user-guides/distributions/#repeatable-randomness-the-salted-seed","title":"Repeatable Randomness (The \"Salted\" Seed)","text":"<p>To make sure your results are repeatable (great for debugging), we use a \"salted\" seeding method. If you provide a single global <code>seed</code>, the system automatically mixes it with other parameters to create a unique local seed for every draw.</p> <p>The three ingredients in the \"Salt\" are:</p> <ol> <li>Global Seed: Controls the broad \"campaign.\" Change this to get a totally different set of results.</li> <li>Distribution Name: Ensures unique draws for different parameters. Without this, two parameters with the same config (like <code>x_offset</code> and <code>y_offset</code>, if using the same dispersion) would produce identical, coupled values.</li> <li>Trial Number: Ensures that every iteration in your Monte Carlo run gets a unique value from the dispersion.</li> </ol>"},{"location":"user-guides/distributions/#the-registry-distributiondict","title":"The Registry: DistributionDict","text":"<p>A <code>DistributionDict</code> acts as a centralized record of the \"rules\" used during a simulation. While a <code>NamedValueDict</code> stores the results (the numbers), the <code>DistributionDict</code> stores the config (the math).</p> <p>This is critical for:</p> <ul> <li>Serialization: Saving exactly what settings were used so a colleague can recreate the simulation.</li> <li>Bulk Updates: Changing the <code>trial_num</code> for every distribution at once as the simulation progresses.</li> </ul>"},{"location":"user-guides/distributions/#supported-distribution-types","title":"Supported Distribution Types","text":"<p>Info</p> <p>Click here to see the technical API for all supported types.</p> <ul> <li>Normal: The classic Bell Curve for natural variation.</li> <li>Uniform: For strict ranges where any value is equally likely.</li> <li>Categorical: To pick from a fixed set of named choices (e.g., Materials).</li> <li>Bernoulli: A simple True/False coin flip.</li> <li>Truncated Normal: A Bell Curve with hard physical limits (e.g., mass cannot be negative).</li> <li>Log Normal: For positive values with \"long-tail\" outliers (e.g., contact forces).</li> <li>Triangular: A simpler alternative to Normal when you only know min, max, and peak.</li> <li>Poisson / Exponential: For modeling the frequency or time between random events.</li> <li>Permutation: To return a shuffled version of a master list.</li> </ul>"},{"location":"user-guides/distributions/#example-sampling-into-registries","title":"Example: Sampling into Registries","text":"<p>When using <code>sample_and_update_dicts</code>, the system checks if a value with that name already exists in your <code>NamedValueDict</code>. If it does, it returns the existing value instead of drawing a new one. This ensures all parts of your simulation use the same \"random\" choice for a single trial.</p> Python<pre><code>from numpydantic import NDArray\n\nimport stochas\n\n# 1. Define the rule\nmotor_rule = stochas.NormalDistribution(\n    name=stochas.DistName(\"motor_torque\"),\n    mu=5.0,\n    sigma=0.2,\n)\n\n# 2. Setup the registries\nrules = stochas.DistributionDict()\nresults = stochas.NamedValueDict[NDArray]()\n\n# 3. Sample and Register\n# This returns a NamedValue and saves it to 'results'\nval_1 = motor_rule.sample_and_update_dicts(\n    dist_dict=rules,\n    named_value_dict=results,\n).squeeze()\n\n# 4. Subsequent calls return the SAME value\nval_2 = motor_rule.sample_and_update_dicts(\n    dist_dict=rules,\n    named_value_dict=results,\n).squeeze()\n\nprint(val_1.value == val_2.value)  # True\n</code></pre>"},{"location":"user-guides/distributions/#example-repeated-sampling","title":"Example: Repeated Sampling","text":"<p>If you need to pull many different random numbers from the same distribution without locking them into a registry (e.g., for noise injection or redraws to meet some constraint), use the <code>.sample()</code> or <code>.draw()</code> methods directly. These move the Random Number Generator forward with every call.</p> Python<pre><code>rules = stochas.DistributionDict()\nresults = stochas.NamedValueDict[NDArray]()\n\nfriction_rule = (\n    stochas.UniformDistribution(\n        name=stochas.DistName(\"friction\"),\n        low=0.2,\n        high=0.4,\n    )\n    .with_seed(42)  # using set seed\n    .with_trial_num(10)  # and set trial_num\n)\n\n# These will all be different random numbers\ndraw_1 = friction_rule.sample_to_named_value().squeeze()  # 0.378\ndraw_2 = friction_rule.sample_to_named_value().squeeze()  # 0.205\ndraw_3 = friction_rule.sample_to_named_value().squeeze()  # 0.216\n\nprint(draw_1.value, draw_2.value, draw_3.value)  # Three different values\n\n# just be sure to add them to a collection when done!\nrules.update(friction_rule)\nresults.update(draw_3)\n</code></pre>"},{"location":"user-guides/named-values/","title":"Named Values","text":""},{"location":"user-guides/named-values/#namedvalues-smart-containers-for-simulation-data","title":"NamedValues: Smart Containers for Simulation Data","text":""},{"location":"user-guides/named-values/#namedvalue","title":"NamedValue","text":"<p>A NamedValue is more than just a variable; it is a smart container designed specifically for the needs of simulation and data tracking. It ensures that once a value is decided for a trial, it stays that way.</p>"},{"location":"user-guides/named-values/#what-is-it","title":"What is it?","text":"<p>In complex simulations, you often need to share a single piece of data (like the mass of a robot arm) across many different functions. A <code>NamedValue</code> acts as the \"Source of Truth\" for that specific parameter.</p> <p>It tracks its own lifecycle using a simple state machine:</p> <ul> <li><code>UNSET</code>: The container is waiting for data. If you try to read it, the system will raise an error to prevent you from using uninitialized data.</li> <li><code>SET</code>: The container has been populated. At this point, it becomes frozen. Any accidental attempt to overwrite it will be blocked unless you explicitly use a \"force\" command.</li> </ul>"},{"location":"user-guides/named-values/#why-use-it","title":"Why use it?","text":"<p>The primary advantage of <code>NamedValue</code> is its Numeric Mixin behavior. Even though it is a complex object that tracks names and states, you can use it in your code as if it were a simple float or a NumPy array.</p> <p>You can do math directly with the container:</p> <ul> <li><code>result = my_named_value * 5.0</code></li> <li><code>total = value_a + value_b</code></li> </ul>"},{"location":"user-guides/named-values/#working-with-collections","title":"Working with Collections","text":"<p>In a real project, you'll likely have dozens of parameters. We use specialized collections to manage them:</p> <ul> <li>NamedValueDict: Best for quick lookups. You can ask for <code>my_dict[\"motor_friction\"]</code> and get the container immediately.</li> <li>NamedValueList: Useful when you need to iterate through all sampled values to save them to a file or a database.</li> </ul> <p>These collections protect their entries from wayward data updates. If a <code>NamedValue</code> with a specific name already exists in the the collection, the value will not be allowed in (without using a force).</p>"},{"location":"user-guides/named-values/#simple-example","title":"Simple Example","text":"Python<pre><code>import stochas\n\n# 1. Initialize a container with a name and a value\n# The state automatically moves to 'SET'\nfriction = stochas.NamedValue(\n    name=stochas.ValueName(\"joint_friction\"), stored_value=0.25\n)\n\n# 2. Use it directly in physics calculations\n# No need to call .value - the mixin handles the math\ntorque_loss = friction * 300\n\n# 3. Manage a group of values\nstate_registry = stochas.NamedValueDict()\nstate_registry.update(friction)\n\n# Access the underlying data safely\ncurrent_friction = state_registry[\"joint_friction\"]\n</code></pre>"},{"location":"user-guides/named-values/#protected-entry-example","title":"Protected Entry Example","text":"<p>Continuing from the previous example</p> Python<pre><code># Updating a NamedValue already in the NamedValueDict\nnew_friction = stochas.NamedValue(\n    name=stochas.ValueName(\"joint_friction\"), stored_value=1234\n)\n\ntry:\n    # \"joint_friction\" is already in state_registry (fails to update)\n    state_registry.update(new_friction)\nexcept Exception:\n    print(\"Failed to update registry!\")\n\n# force the value in anyway\nstate_registry.force_update(new_friction)\n</code></pre>"},{"location":"user-guides/orchestration/","title":"Orchestration","text":""},{"location":"user-guides/orchestration/#stochasbase-the-simulation-conductor","title":"StochasBase: The Simulation Conductor","text":"<p><code>StochasBase</code> is the central \"brain\" for a simulation trial. It orchestrates the lifecycle of a model by bridging the gap between mathematical uncertainty and deterministic execution.</p>"},{"location":"user-guides/orchestration/#centralized-trial-orchestration","title":"Centralized Trial Orchestration","text":"<p>A single instance of StochasBase manages two distinct pillars of simulation inputs:</p> <ul> <li>Aleatory Uncertainty (<code>sample_dist</code>): Represents \"luck\" or noise. These are random draws from probability distributions (Normal, Uniform, etc.) that you cannot control but must account for.</li> <li>Epistemic Uncertainty (<code>sample_design</code>): Represents \"choices.\" These are tunable parameters (Design Variables) used by optimizers like Optuna or pymoo to find peak performance.</li> </ul>"},{"location":"user-guides/orchestration/#the-pillars-of-repeatability","title":"The Pillars of Repeatability","text":"<p>To ensure that a simulation can be perfectly recreated by a colleague, the orchestrator utilizes a \"Salted Seed\" logic. Every random draw is a deterministic function of the trial's metadata:</p> \\[Seed_{local} = \\mathcal{H}(Seed_{global}, Trial_{num}, Variable_{name})\\] <ul> <li>Global Seed: Controls the entire campaign.</li> <li>Trial Number: Ensures trial 10 is different from trial 11.</li> <li>Variable Name: Ensures \"stiffness\" doesn't get the same random value as \"damping.\"</li> </ul>"},{"location":"user-guides/orchestration/#the-baked-registry-named","title":"The \"Baked\" Registry (named)","text":"<p>While <code>StochasBase</code> is designed to automate randomness and optimization, you often need to bypass the math to test a specific \"Golden Case\" or debug a known failure point. Overrides allow you to inject fixed values into the Baked Registry before the simulation begins.</p> <p>The orchestrator follows a strict priority sequence when a variable is requested:</p> <ol> <li>Registry Check: It looks in the <code>named</code> dictionary. If a value exists, it returns it immediately.</li> <li>Logic Execution: If the registry is empty, it proceeds to draw from a distribution or ask an optimizer for a suggestion.</li> </ol> <p>By using <code>with_overrides()</code>, you populate the registry early, effectively \"locking\" those variables for the duration of the trial.</p>"},{"location":"user-guides/orchestration/#quick-implementation","title":"Quick Implementation","text":"Python<pre><code>import numpy as np\n\nimport stochas\n\n\nclass MyModel(stochas.StochasBase):\n    \"\"\"Put your logic for running your model here!\"\"\"\n\n\noverrides = stochas.NamedValueDict()\noverrides.update(\n    stochas.NamedValue(\n        name=stochas.ValueName(\"overridden_value\"), stored_value=np.array([3.14])\n    )\n)\n\nmodel = MyModel().with_seed(42).with_trial_num(1).with_overrides(overrides)\n\n# 1. Random draw\nnoise = model.sample_dist(\n    stochas.NormalDistribution(\n        name=stochas.DistName(\"sensor_noise\"), nominal=0, mu=0, sigma=0.1\n    )\n)\n\n# 2. Tunable parameter\nwidth = model.sample_design(\n    stochas.DesignFloat(\n        name=stochas.ValueName(\"base_width\"), low=1.0, high=5.0, stored_value=2.5\n    )\n)\n\n# Both are now registered in model.named for later analysis.\n\n# fails to update named value dict! overridden_value is already in the named value dict\noverridden_value = model.sample_dist(\n    stochas.PoissonDistribution(name=stochas.DistName(\"overridden_value\"), lam=4)\n)\nassert overridden_value == np.array([3.14])  # this check passes!\n</code></pre>"}]}